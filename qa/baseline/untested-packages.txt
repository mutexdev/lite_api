# Packages with no _test.go files of their own, and why each is acceptable.
# qa/test-presence.sh fails if this list and reality disagree in either
# direction — a package that silently loses its tests, or one that gains them
# and is not removed from here.
#
# docs/qa/fixtures        a fixture package: data for other tests to read
# .../flatted             vendored third-party Go inside node_modules, swept up
#                         by ./... and nothing to do with this project
# tools/extraction        build-ignored tooling for the extraction work
docs/qa/fixtures
frontend/node_modules/flatted/golang/pkg/flatted
tools/extraction
# The repository root, which holds main.go and nothing else. It is 22 lines:
# the embedded frontend bundle — which cannot move, because //go:embed resolves
# relative to its own file — and a call into internal/core.Run. Everything it
# does needs a live Wails runtime, so there is nothing here a test can reach.
# The logic it used to hold now lives in internal/core and is tested there.
.
