# LiteAPI Bruno Parity Ledger

This app is a Go + Svelte + Wails rebuild of Bruno. The authoritative reference is `/Users/mou/Developer/Workspace/bruno` plus the installed `/Applications/Bruno.app` UI.

## Computer Evidence

- LiteAPI renders as a desktop app with workspace rail, collection list, request tabs, method/URL bar, Send/Run controls, response pane, editable request vars, and response view selector.
- LiteAPI Computer UI smoke selected the plain `Echo JSON` request tab, switched the request pane to Body and the response pane to Headers, restarted Wails, and verified the normal request tab restored with Body active and the response headers table visible, matching Bruno's per-tab UI-state snapshot behavior.
- LiteAPI Computer UI smoke opened a saved response example's Edit details view, verified edit-mode-only `Drag row/file to reorder` handles across file body, params, request headers, and response headers, and verified response headers render without request-style enabled checkboxes.
- LiteAPI Computer UI smoke opened a saved response example editor, changed the response `Content-Type` header from `application/json` to `text/html` through the visible table inputs, and verified the example response body type changed from `json` to `html`.
- LiteAPI Computer UI smoke opened an isolated collection with collection/folder/request post-response variables, sent `GET {{host}}/capture` against a local JSON endpoint, verified `200`, and verified the response Tests tab showed request, folder, and collection tests passed after reading the persisted runtime vars.
- LiteAPI Wails dev browser smoke opened an isolated collection, clicked the sidebar folder row into Collection > Folder Settings, edited folder Headers, Vars pre-request/post-response, Basic Auth, Script, Test, and Docs through the rendered controls, verified `folder.bru` persisted each Bruno section, sent the request against a local verifier, displayed `200`, and verified the response Tests tab showed `folder settings UI passed passed`. Direct native Computer capture listed `LiteAPI` at bundle id `com.wails.LiteAPI` but returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke created a collection folder through `collection-actions-new-folder`, verified Bruno's `New Folder` modal, auto-derived filesystem folder name, `New folder created!` state notification, selected Folder Settings view, sidebar empty-folder row, and `folder.yml` with `info.name`, `info.type: folder`, `seq`, and `request.auth: inherit`; the same smoke used the folder-row `collection-item-menu-new-folder` action to create a nested folder and verified the nested sidebar row plus nested `folder.yml`. Native Computer capture remains blocked by `cgWindowNotFound` for this Wails app, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened a folder-row `collection-item-menu-rename` action, verified Bruno's `Rename Folder` modal with `#collection-item-name`, changed only the display name while keeping `#file-name` unchanged, verified the directory stayed in place and `folder.yml` metadata changed, then renamed again with the auto-derived filesystem folder name, verified the directory moved, sidebar row updated, `Item renamed successfully` state notification existed, and `folder.yml` retained inherited auth. Native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened a folder-row `collection-item-menu-clone` action, verified Bruno's `Clone Folder` modal with `#collection-item-name`, default `<name> copy` behavior, filesystem option showing the tracked name before switching to editable `#file-name`, submitted through `clone-item-button`, verified the cloned root and nested folder rows appeared without opening a folder tab, and confirmed cloned nested folder/request files existed on disk with the custom filesystem folder name. Native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened folder-row and request-row `collection-item-menu-info` actions, verified Bruno's close-only `Info` modal, confirmed Folder rows show only `Folder Name` and `Folder Name (on filesystem)`, Request rows show only `Request Name` and `File Name (on filesystem)`, confirmed no footer submit button and no uid/path/seq/date metadata, and closed through `modal-close-button`. Native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke clicked folder-row and request-row `collection-item-menu-show-in-folder`, verified the macOS `Reveal in Finder` label from Bruno's platform helper, confirmed request rows do not expose `collection-item-menu-open-terminal`, clicked folder-row `collection-item-menu-open-terminal`, verified Dev Tools Terminal opened with a session whose cwd was the folder's physical filesystem path rather than its display name, and captured `/tmp/liteapi-item-actions-ui-smoke.png`. Bruno native Computer `get_app_state` timed out during this slice, so Bruno parity facts came from source inspection and LiteAPI used the Wails browser endpoint with reveal dry-run enabled.
- LiteAPI Wails dev browser smoke clicked an HTTP request-row `collection-item-menu-generate-code` action, verified Bruno's `Generate Code` modal title, generated an interpolated cURL snippet from current draft request state with inherited collection header and disabled rows omitted, switched `request-code-language` to JavaScript fetch, verified the fetch snippet, confirmed a gRPC row does not expose `collection-item-menu-generate-code`, and captured `/tmp/liteapi-request-code-ui-smoke.png`. Bruno parity facts came from source inspection because native Computer capture has been unreliable for this app family.
- LiteAPI Wails dev browser smoke opened a request-row `collection-item-menu-clone` action, verified Bruno's `Clone Request` modal with `#collection-item-name`, default `<name> copy`, filesystem option plus editable `#file-name`, submitted through `clone-item-button`, verified the cloned request row and active cloned tab, confirmed `Request cloned!`, and confirmed the custom `.yml` request file existed on disk with cloned URL content. Native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened a request-row `collection-item-menu-rename` action, verified Bruno's `Rename Request` modal with `#collection-item-name`, filesystem option, editable `#file-name` through `rename-request-edit-icon`, submitted through `rename-item-button`, and verified the same request id/active tab, old request file removal, new `.yml` file write, and `Item renamed successfully` notification. Native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened a request-row `collection-item-menu-delete` action, verified Bruno's `Delete Request` modal and `Are you sure you want to delete <name> ?` copy, clicked `modal-submit-btn`, verified the request row/state entry and `.yml` file disappeared, confirmed the request tab closed while the response-example tab for that deleted request stayed open and active, and verified success remained silent with no new notification. Native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened a folder-row `collection-item-menu-delete` action, verified Bruno's `Delete Folder` modal and `Are you sure you want to delete Delete Parent ?` copy, clicked `modal-submit-btn`, verified the parent and nested folder rows disappeared from the rendered sidebar, confirmed state no longer contained `delete-parent` paths, and confirmed the recursive folder directory was gone from disk. Native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened Preferences, selected app proxy mode `PAC`, entered a `file://` PAC source pointing at a local PAC file, verified Collection > Proxy remained `Inherit`, sent `GET /proxy-prefs` against a local target, displayed `200` with `"proxied": true`, and verified local proxy/target logs showed exactly one absolute-form proxy request and the proxy-added marker header at the target. A second PAC smoke used a conditional `FindProxyForURL(url, host)` with `shExpMatch` and `isInNet`, sent `/proxied` and `/direct` through the rendered request UI, and verified only `/proxied` hit the proxy. The in-app browser controller was listed but did not expose the documented Playwright control surface in this runtime, so the smokes used the Wails browser endpoint through Chrome DevTools.
- LiteAPI native Computer UI smoke selected the Bruno-style `Scratch` collection, verified the `SCRATCH` badge and temp directory path, created `Scratch Smoke` through the sidebar request controls, verified the request row showed `temp draft`, clicked `Save temp` and verified `draft` cleared while `temp` remained, sent `GET http://127.0.0.1:18080/scratch-smoke`, displayed `200` with `"scratch": true`, and confirmed the local server received `GET /scratch-smoke`. Shell evidence showed `Scratch Smoke.yml`, `metadata.json`, and `opencollection.yml` only under `/tmp/liteapi-scratch-ui-smoke/data/transient/bruno-scratch-*`, while persisted `state.json` excluded scratch collection ids/paths/request tabs.
- LiteAPI native Computer UI smoke opened Preferences > Appearance, verified Bruno-style Light/Dark/System mode controls and all Bruno light/dark theme variant cards including Catppuccin and Nord, toggled Light and saw the main pane switch to light styling, toggled Dark and selected Nord, saw the app switch to the Nord blue-gray palette, and verified the isolated `state.json` persisted `theme: dark` and `themeVariantDark: nord`.
- LiteAPI native Computer UI smoke opened Preferences > Keybindings, verified Bruno-style grouped command rows/defaults and the Enabled toggle, recorded `Cmd+Shift+K` for Global Search through the shortcut field, verified `state.json` persisted `command+bind+shift+bind+k`, fired the new shortcut to open/focus Global Search, disabled keybindings, and verified the same chord no longer opened the modal.
- LiteAPI backend/frontend coverage now wires Bruno-style tab-management and app-navigation keybindings behind the Preferences table: save active tab, Save All Tabs scoped to the active collection, close current/all tabs, reopen last closed tab, switch previous/next/last/tab 1-8, move active tab left/right, open Import, and open Environments. `TestOpenTabManagementPersistsOrderAndActiveState` covers persisted tab order, active-tab fallback after close, close-all, scoped/global LIFO reopen history capped like Bruno, stale closed-tab pruning, and invalid tab errors. `TestSaveAllTabsWritesOpenRequestTabs` and `TestSaveAllTabsAssignsUniquePathsForDuplicateRequestNames` cover active-collection scoping, open request-tab persistence, draft clearing, and duplicate request filename assignment.
- LiteAPI Wails dev browser smoke pressed Bruno-style shortcuts through the rendered UI: `Cmd+N` created request tabs, `Cmd+2` selected the second tab, `Cmd+9` selected the last tab, `Cmd+[`/`Cmd+]` moved the active tab left/right without changing count, `Cmd+W` closed the active app tab, `Cmd+Shift+W` cleared all app tabs, and `Cmd+O`/`Cmd+E` switched to Import and Environments.
- LiteAPI Wails dev browser smoke pressed `Cmd+N` twice to create request tabs, `Cmd+W` to close the active tab, then `Cmd+Shift+T` to reopen the last closed tab; the top tab strip returned to three tabs with the reopened `New Request` active, and isolated `state.json` confirmed `activeTabId` pointed at the restored tab while `closedTabs` drained to `[]`.
- LiteAPI Wails dev browser smoke pressed `Cmd+N` twice, edited both duplicate `New Request` tabs through the visible URL field, pressed `Cmd+Shift+S`, and verified the active collection wrote both requests to distinct files: `New Request.yml` with `https://example.test/save-all-one` and `New Request 2.yml` with `https://example.test/save-all-two`, with `state.json` showing both draft flags cleared and file paths assigned.
- Bruno source inspection confirmed Change Orientation is `changeLayout`, default `Cmd+J`/`Ctrl+J`, toggling persisted `preferences.layout.responsePaneOrientation` between `horizontal` and `vertical`. LiteAPI Wails dev browser smoke started horizontal, pressed `Cmd+J` to stack request above response, clicked the response layout toggle back to horizontal and again to vertical, verified `state.json` persisted `responsePaneOrientation: vertical`, restarted Wails, and verified the workbench restored vertical.
- Bruno source/test inspection confirmed Collapse Sidebar is `collapseSidebar`, default `Cmd+\`/`Ctrl+\`, toggling non-persisted `state.app.sidebarCollapsed` so the full left `aside.sidebar` width moves between `250px` and `0px`. LiteAPI Wails dev browser smoke verified the rail started expanded at `312px`, clicked `toggle-sidebar-button` to collapse to computed `0px` and expand back to `312px`, then pressed `Cmd+\` to collapse to `0px` and again to expand to `312px`.
- Bruno source inspection confirmed `zoomIn`/`zoomOut`/`resetZoom` default to `Cmd+=`/`Cmd+-`/`Cmd+0` on macOS and `Ctrl+=`/`Ctrl+-`/`Ctrl+0` elsewhere, persist `display.zoomPercentage`, step by 10%, clamp 50-150%, and expose fixed 50-150% Preferences options. LiteAPI Wails dev browser smoke opened Preferences > Display, verified options/default `100%`, selected `130%` and saw `--app-zoom: 1.3` plus persisted `state.json`, pressed `Cmd+=` to `110%`, `Cmd+-` back to `100%`, `Cmd+0` reset from `130%`, then reloaded at `120%` and verified it restored from disk.
- Bruno source inspection confirmed Preferences > Display > Font stores free-form `font.codeFont` with default `default` plus `font.codeFontSize` default `13`, clamped `1..32`, and passes both into CodeMirror-style editors. LiteAPI Wails dev browser smoke opened Preferences > Display, verified `Code Editor Font`/`Font Size` defaults and root editor variables, entered `Courier New` and `18`, saw `--code-font-family` and `--code-font-size: 18px` update live, verified `state.json` persisted nested `font` plus the flat legacy size mirror, entered `40` and saw clamp to `32px`, then restored `18` and verified reload persistence.
- Bruno source inspection confirmed `closeBruno` defaults to `Cmd+Q` on macOS and `Ctrl+Shift+Q` elsewhere. LiteAPI Wails dev browser smoke pressed `Cmd+Q` through the rendered page in an isolated dev session and verified the Wails dev process exited cleanly.
- LiteAPI native Computer UI smoke used the Cookies screen to save `notif=one` for `api.example.com`, verified the topbar `Check all Notifications` trigger displayed unread badge `1`, opened the Notifications modal, verified All/Unread tabs, selected `Cookie saved` detail with `Success`, saw the item auto-mark read as `0 unread`, verified the Unread empty state, clicked Clear all, and confirmed persisted `state.json` had `"notifications": []`.
- LiteAPI native Computer UI smoke verified Bruno-style support/licensing app-shell surfaces: the rail footer exposed Support and Golden Edition, Support opened a modal with Documentation, Report Issues, Discord, GitHub, and Twitter links, Golden Edition opened with the Buy button, Individuals `$19` pricing and Bruno individual feature list, Organizations switched to `$49 / user` plus organization features including Priority Support, Preferences rendered the same Support link list, and Global Search showed/filter-matched `Bruno Documentation` with `Browse the official Bruno documentation`.
- LiteAPI native Computer UI smoke sent `GET http://127.0.0.1:18080/example`, opened Dev Tools > Network, verified Bruno-style Method filters with `Hide All`/`Show All`, seven method checkboxes and counts, Network columns `Method`/`Status`/`Domain`/`Path`/`Time`/`Duration`/`Size`, status sorting through ascending and descending states, selected-row highlighting, captured response size `49 B`, and Request Details tabs for Request headers/body, Response headers/body including `X-Devtools-Smoke: yes`, and Network Logs with started time, duration, and size.
- LiteAPI native Computer UI smoke sent a request in an isolated app data directory, opened Dev Tools > Network, dragged the Network column divider to persist custom Bruno-style widths `[80,70,232,248,110,100,80]`, restarted Wails, and verified the app reopened directly to Dev Tools > Network with the captured request row, Request Details panel, and persisted table geometry; disk state confirmed `devTools.open: true`, `activeTab: network`, and `detailsPanelWidth: 400`.
- LiteAPI Wails dev browser smoke opened Dev Tools from the Request workbench as a bottom drawer, verified Request and Dev Tools were both active, confirmed the old full-page DevTools branch was not rendered, dragged the drawer from `320px` to `425px`, verified the request URL remained visible above it, restarted Wails with the same isolated data directory, and verified the drawer reopened at `425px` with `devTools.open: true` and `drawerHeight: 425` persisted on disk.
- Bruno reference Computer check confirmed Dev Tools exposes Console/Network/Performance/Terminal tabs, the Terminal tab exposes Sessions, New Terminal Session, `No active sessions`, and `No terminal session selected`; LiteAPI native Computer UI smoke then opened Dev Tools > Terminal, created a PTY-backed session at `/tmp/liteapi-terminal-ui-smoke`, verified the session row and PID, sent `printf 'terminal-ui-smoke\n'` through the Terminal input, saw `terminal-ui-smoke` in the cleaned terminal output, and closed the session back to the empty state.
- Bruno source inspection confirmed the collection menu `Open in Terminal` action opens Dev Tools Terminal at the collection cwd. LiteAPI Wails dev browser smoke created an isolated collection, materialized its folder, clicked Collection overview `Open in Terminal`, verified the Dev Tools terminal session row used `/tmp/liteapi-open-terminal-ui-smoke/My Workspace/<collection>`, sent `pwd && printf 'open-terminal-ui-smoke\n'`, and confirmed the cleaned terminal output contained both the collection path and smoke marker.
- Bruno source inspection confirmed the collection menu `Show in Folder` action uses macOS label `Reveal in Finder` and reveals the collection path through the OS shell. LiteAPI backend tests verify platform command selection, collection path validation, and missing-folder guards; a dry-run Wails browser UI smoke clicked Collection overview `Reveal in Finder` for `/tmp/liteapi-reveal-ui-smoke/My Workspace/<collection>` and verified the success status.
- Bruno source inspection confirmed `Remove` unmounts a collection from the workspace without deleting files. LiteAPI backend tests verify Remove clears open tabs and preserves the collection directory, rejects scratch collections, and the Wails browser UI smoke removed an isolated collection through the confirmation modal while confirming `remove-keeps-files.txt` remained on disk.
- Bruno source inspection confirmed `Rename` updates collection metadata without moving the collection folder and writes only `opencollection.yml` or `bruno.json`. LiteAPI backend tests verify YAML and BRU metadata updates, unchanged paths, empty-name rejection, whitespace preservation, and no `collection.bru` write on rename; the Wails browser UI smoke created an isolated collection, clicked Collection overview `Rename`, submitted the modal, verified the sidebar/workbench state updated under the same path, and confirmed `opencollection.yml` contained the new name.
- Bruno source inspection confirmed `Clone` opens a `Clone Collection` modal, defaults to `<name> copy`, creates a new folder, writes a renamed root `opencollection.yml`/`bruno.json`, recursively copies only matching `.yml` or `.bru` files, and skips the source root config. LiteAPI backend tests cover YAML/BRU clone file-copy/exclusion, existing-target rejection, invalid folder-name rejection, source preservation, and root-config name precedence; the Wails browser UI smoke created a source collection, opened Collection overview `Clone`, verified the modal defaults, submitted `Create`, saw the cloned collection selected under a distinct path, and confirmed the cloned `opencollection.yml` contained the clone name.
- Bruno reference Computer check confirmed Dev Tools > Performance renders a `View:` selector set to `Cumulative (All Processes)` plus numeric CPU Usage, Memory Usage, Uptime, and Process ID cards; LiteAPI native Computer UI smoke then opened Dev Tools > Performance in `/tmp/liteapi-cpu-ui-smoke`, verified CPU Usage showed a numeric percent instead of `N/A`, opened the selector, selected `PID 33307 - LiteAPI (main)`, verified the process-specific CPU/memory/uptime/PID cards stayed populated, and clicked Refresh to update the runtime snapshot.
- LiteAPI UI smoke sent `POST {{host}}/post` to `https://httpbin.org/post` and displayed `200`, timing, size, and formatted JSON response.
- LiteAPI UI smoke opened `/Users/mou/Documents/bruno/Sample API Collection` legacy OpenCollection YAML, showed `GET Get Users`, sent `https://jsonplaceholder.typicode.com/users`, and displayed `200`, timing, size, and formatted JSON response.
- LiteAPI UI smoke opened a `.bru` collection with `collection.bru` and `environments/Local.bru`, sent `GET {{host}}/get`, resolved `host` from collection variables, applied inherited `X-Collection-Smoke`, displayed `200`, and showed the loaded `Local` environment with `apiToken` marked secret.
- LiteAPI UI smoke opened the new Collection view for that `.bru` collection and verified Overview docs/path, Headers, and Vars tabs render imported collection metadata.
- LiteAPI UI smoke opened a nested `.bru` collection with `folder.bru` and `folder.yml`, rendered the `Users/v1` folder label in the sidebar, sent the nested request, resolved collection variables, and displayed `200`.
- LiteAPI UI smoke opened a `.bru` collection whose root auth was intentionally wrong and whose `folder.bru` supplied the correct Basic auth plus a URL variable, rendered the `Secure` folder label, sent `GET {{host}}/{{route}}`, resolved `route` to `basic-auth/folder-user/folder-pass`, and displayed `200` with `authenticated: true`.
- LiteAPI UI smoke filtered the sidebar with request search text, kept matching collection/folder/request rows visible, reported the matching request count, and showed an empty state for a miss.
- LiteAPI Wails dev browser smoke opened Bruno-style Global Search with `Cmd+K`, verified the input focus and default collection result, searched across request type/name/URL/path data, used ArrowDown/ArrowUp to move the active row, pressed Enter to open `Echo JSON`, reopened from the Global Search button, verified a no-match empty state, and pressed Escape to close without changing the active request. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI UI smoke sent the sample request, used the response Example command, switched to the Examples tab, and rendered the captured response as `Echo JSON example 1` with its status/body.
- LiteAPI Wails dev browser smoke sent the sample request to a local `127.0.0.1:18795` JSON server, saved the live response as an example, renamed it to `Renamed UI Example`, cloned it as `Renamed UI Example (Copy)`, deleted the clone through confirmation, and verified the Examples tab retained the renamed original with the captured response body. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke saved the current response as a response example, clicked the example `Generate Code` action, verified the cURL snippet used the saved request snapshot URL and headers, switched the language selector to JavaScript fetch, and verified the fetch snippet rendered the same saved request snapshot. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI UI smoke opened a `.bru` collection with JavaScript `script:pre-request`, `script:post-response`, and `tests` blocks, showed the on-disk URL as `/wrong`, sent the request, executed the pre-request JS to send `POST /scripted` with a scripted header, displayed `202`, and showed Bruno-style `test(...)` results `js status passed` and `post var passed`.
- Bruno reference tests/source for Response Timeline confirmed scripted `bru.sendRequest` rows render newest-first with `All`/`Request`/`Pre-Request` filter chips, request/sendRequest badges, expandable row details, and source-file attribution; LiteAPI native Computer UI smoke edited the request Pre-request script to call `bru.sendRequest` against `127.0.0.1:18080/pre`, sent the main request to `/main`, verified the Timeline tab showed `All 2`, `Request 1`, `Pre-Request 1`, `/main` request and `/pre` sendRequest rows, filtered to the Pre-Request row, expanded it, and verified `Echo JSON.yml` source detail while the loopback server logged `GET /pre` then `POST /main`.
- Bruno reference tests/source for nested Response Timeline confirmed `bru.runRequest` rows use the runRequest badge while counting under the calling script phase, and inner scripted requests bubble to the outer request Timeline; LiteAPI native Computer UI smoke created an `Inner` request through the UI, set its Pre-request script to call `bru.sendRequest` against `127.0.0.1:18080/inner-pre`, set the outer request Pre-request script to `bru.runRequest("Inner")`, sent `POST /outer`, verified the loopback server logged `GET /inner-pre`, `GET /inner`, then `POST /outer`, verified the Timeline tab showed `All 3`, `Request 1`, `Pre-Request 2`, outer request, inner runRequest, and bubbled inner sendRequest rows, filtered to Pre-Request, and expanded both scripted rows to verify `Inner.yml` source detail.
- Bruno source confirmed OAuth2 token fetches are recorded as Timeline `oauth2.0` rows under the OAuth filter; LiteAPI native Computer UI smoke configured OAuth2 `client_credentials` against a local `127.0.0.1:18080` token/resource server, clicked Send, verified the loopback log showed `POST /token` before `POST /resource`, displayed `200` with `Bearer ui-oauth-token`, verified the Timeline tab exposed `OAuth 1` and `/token oauth2.0`, filtered to OAuth, and expanded the token row to verify `Echo JSON.yml`, `POST http://127.0.0.1:18080/token -> 200`, and duration detail.
- LiteAPI UI smoke opened a Cookie Manager `.bru` collection, sent `GET /set`, captured `session=abc123`, rendered it in Cookies with `127.0.0.1`, `/`, `session`, and `httpOnly, hostOnly`, sent `GET /check` with the stored cookie and displayed `200` plus `cookie: true`, deleted the row in Cookies, then re-sent `GET /check` and displayed `401` plus `cookie: false`.
- LiteAPI UI smoke opened an isolated Cookies view, manually added `manual=one` under `api.example.com` with `/api` and `httpOnly, hostOnly`, imported raw `raw=ok` from `https://api.example.com/v1/list` with `/v1`, `secure`, `sameSite=lax`, filtered search to `raw` and `manual`, opened Edit cookie for `manual`, saved it as `manual=two`, and verified the grouped preview/table updated.
- LiteAPI Computer UI smoke opened an isolated Cookies view seeded with `api.example.com` and `other.example.com`, clicked `Clear domain` on `api.example.com`, and verified `api.example.com` disappeared while `other.example.com` with `other=two` remained and the count changed to `1/1 stored cookies`.
- LiteAPI Wails dev browser smoke opened an isolated Cookies view, saved `ui_cookie=ui-secret-cookie` for `api.example.com` through the Add cookie form, verified the cookie row rendered in the UI, and inspected `state.json` to confirm the plaintext `ui-secret-cookie` was absent while the persisted cookie value used `$01:` encrypted storage. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI UI smoke opened an isolated OAuth2 credential-variable request against a local `127.0.0.1:18743` token/resource server, sent `GET /resource`, fetched and applied `Bearer ui-access`, displayed `200`, and showed `res.status passed`, `oauth2 credential vars visible passed`, and `reset oauth2 credentials passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated Digest Auth request against a local `127.0.0.1:18781` Digest challenge server, verified the Auth tab showed mode `digest`, username `digest-user`, and a populated password field, clicked Send, observed the verifier's `401` then `200` challenge/retry flow, displayed `200` with `authenticated: true` and `user: digest-user`, and showed `res.status passed`, `digest status visible passed`, and `digest body visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated WSSE Auth request against a local `127.0.0.1:18782` verifier, verified the Auth tab showed mode `wsse`, username `wsse-user`, and a populated password field, clicked Send, verified the Bruno-compatible `X-WSSE` UsernameToken digest server-side, displayed `200` with `wsse: true`, `user: wsse-user`, and `nonceLength: 32`, and showed `res.status passed`, `wsse status visible passed`, and `wsse body visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated OAuth1 Auth request against a local `127.0.0.1:18783` verifier, verified the Auth tab showed mode `oauth1`, consumer key `consumer_key`, token `access_token`, signature `HMAC-SHA1`, header placement, fixed timestamp/nonce/version/realm, clicked Send, verified the OAuth Authorization header signature server-side against the URL query and form body, displayed `200` with `oauth1: true`, `consumerKey: consumer_key`, `signatureMethod: HMAC-SHA1`, and `body: foo=bar&z=last`, and showed `res.status passed`, `oauth1 status visible passed`, and `oauth1 body visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated AWS SigV4 Auth request against a local `127.0.0.1:18784` verifier, verified the Auth tab showed mode `awsv4`, access key `AKIDEXAMPLE`, masked secret/session token, service `execute-api`, region `us-east-1`, and empty profile, clicked Send, verified the AWS4-HMAC-SHA256 Authorization signature server-side against the URL query, JSON body, `content-type`, `x-amz-content-sha256`, `x-amz-date`, `x-amz-meta-test`, and `x-amz-security-token`, displayed `200` with `signed: true`, `accessKeyId: AKIDEXAMPLE`, `service: execute-api`, `region: us-east-1`, and no failures, and showed `res.status passed`, `aws sigv4 status visible passed`, and `aws sigv4 body visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated NTLM Auth request against a local `127.0.0.1:18785` challenge verifier, verified the Auth tab showed mode `ntlm`, username `alice`, masked password, and domain `DOMAIN`, clicked Send, observed the server-side `401` no-auth challenge, `401` Type 1 negotiate challenge, and final `200` Type 3 authenticate flow with JSON body replay on every call, parsed the Type 3 message as user `alice` and domain `DOMAIN`, displayed `200` with `ntlm: true`, `messageType: 3`, `calls: 3`, `noauth: 1`, `negotiate: 1`, `authenticate: 1`, and no failures, and showed `res.status passed`, `ntlm status visible passed`, and `ntlm body visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated Git-backed workspace seeded with a normal collection, a missing-folder ghost collection, and a local `file:///tmp/liteapi-git-ui-smoke/source` git repo containing a `Git API` Bruno collection; verified the ghost row rendered `GIT NOT CLONED` with remote URL and Remove control, connected `Sample API` to the file remote from Collection overview, verified the sidebar `GIT` badge, persisted `remote`, and managed `.gitignore` `/Sample API` entry, removed the remote through the UI and verified the badge/remote and `.gitignore` entry cleared, removed the ghost row and verified it disappeared from state, then used Import > Git Repository to check `git --version`, clone the local repo, display the `Git API` candidate table row with format `bru`, `1` request, selected checkbox, and cloned path, opened selected, and verified the `Git API` collection mounted with `GIT BRU`, request `GET Ping`, remote URL, cloned path, and a managed `.gitignore` entry for the cloned collection.
- LiteAPI Wails dev browser smoke opened Import > Git Repository against a local `file:///tmp/liteapi-git-progress-smoke-l7orq2/source` repo, cloned to an isolated folder, verified the progress log rendered queued, checking, preparing, cloning, streamed `Cloning into ...` output, scanning, and done rows, and verified the `Progress API` candidate table row showed format `bru`, `1` request, and the cloned path. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- Bruno source inspection confirmed OpenAPI sync stores `brunoConfig.openapi`, caches source specs under user data `specs`, rejects Swagger 2.0, diffs endpoints by normalized method/path, and preserves user scripts/tests/assertions and request values while applying spec shape. LiteAPI Wails dev browser smoke imported an OpenAPI 3.0 collection with `Check for Spec Updates`, opened Collection > OpenAPI Sync, pasted a v2 spec override, clicked Connect, Check Updates, and Apply Sync, verified counts moved from `1 added · 1 updated · 1 removed · 0 unchanged` to `0 added · 0 updated · 1 removed · 2 unchanged`, and verified the new `List owners` request appeared; screenshot artifact: `/tmp/liteapi-openapi-sync-ui-smoke.png`. Native Computer capture remains unreliable for this Wails app family, so this smoke used the in-app browser controller.
- Bruno source inspection confirmed OpenAPI sync review decisions use `keep-mine` and `accept-incoming`, default added/updated/removed spec changes to accepting the spec, and expose `Skip All`/`Accept All` review controls. LiteAPI Wails dev browser smoke opened the OpenAPI Sync review table with one added, one updated, and one removed endpoint, verified `Skip All` and `Accept All`, set the updated endpoint to `Keep Current`, set the added endpoint to `Skip`, left the removed endpoint as `Delete`, clicked Apply Sync, verified the preview became `1 added · 1 updated · 0 removed · 0 unchanged`, and verified the sidebar kept `Get pets`, skipped `List owners`, and removed `Legacy request`; screenshot artifact: `/tmp/liteapi-openapi-review-ui-smoke.png`.
- Bruno source inspection confirmed OpenAPI Collection Changes compares the current collection to the last stored spec, reports modified/deleted/added local drift, ignores value-only request edits, and exposes Reset/Restore/Delete plus bulk revert actions. LiteAPI Wails dev browser smoke imported a synced v1 spec, applied a v2 spec while keeping one updated endpoint and skipping one added endpoint, clicked Collection Changes > Check Collection, verified `1 modified · 1 deleted · 0 added` with Reset and Restore rows, clicked `Revert All to Spec`, and verified `0 modified · 0 deleted · 0 added` with both spec endpoints restored; screenshot artifact: `/tmp/liteapi-openapi-drift-ui-smoke.png`.
- Bruno source inspection confirmed OpenAPI Connection Settings persists per-collection `sourceUrl`, `autoCheck`, and `autoCheckInterval`, defaults auto-check to 5 minutes, and background polling does a lightweight remote hash check before surfacing update state. LiteAPI Wails dev browser smoke imported a synced OpenAPI collection, opened Collection > OpenAPI Sync > Settings, saved auto-check disabled and verified `Auto Check for Updates: Disabled`, reopened Settings, selected `15 min`, saved, verified `Auto Check for Updates: Every 15 min · No updates`, and confirmed `bruno.json` persisted `"autoCheck": true` plus `"autoCheckInterval": 15`; screenshot artifact: `/tmp/liteapi-openapi-settings-ui-smoke.png`.
- Bruno source inspection confirmed OpenAPI Sync `View spec` opens an `API Spec` special tab, reads the cached spec first, falls back to fetching the configured source if the cache is missing, pretty-prints JSON specs, and marks fallback specs as remote. LiteAPI now exposes Collection > OpenAPI Sync > `View spec`, reads the cached spec with the same source fallback semantics, displays the source/origin and spec content in a read-only viewer modal, and browser-smoked a synced OpenAPI import showing `Spec Viewer Smoke API` plus `/smoke` from the stored spec; screenshot artifact: `/tmp/liteapi-openapi-spec-ui-smoke.png`.
- Bruno source inspection confirmed OpenAPI Sync `View Spec Diff` opens a spec diff modal with Current Spec and Updated Spec panes plus added/updated/removed badges. LiteAPI now exposes Collection > OpenAPI Sync > `View Spec Diff`, compares the stored cached spec with the configured incoming source or override, surfaces endpoint counts and line-level current/updated spec changes, and browser-smoked a synced v1 spec with a configured v2 file source showing `1 added · 1 updated · 0 removed · 0 unchanged`, old/new version lines, `/owners`, and `page`; screenshot artifact: `/tmp/liteapi-openapi-diff-ui-smoke.png`.
- Bruno source inspection confirmed `SpecDiffModal` has `Previous`/`Next` change navigation with a change counter, and Collection Changes modified/locally-added rows expose `Open` actions while Spec Updates rows do not. LiteAPI now adds Spec Diff previous/next navigation with active changed-line highlighting, and Collection Changes `Open` actions for modified/local-only rows with existing request ids. The Wails browser smoke imported synced v1 with a configured v2 source, opened Spec Diff, verified `1 / 16 changes` with Previous disabled and then `2 / 16 changes` after Next, edited a request query param through the UI, ran Collection Changes, verified `1 modified · 0 deleted · 0 added` with one `Open`, clicked it, and verified the request editor opened with `localOnlyParam`; screenshot artifacts: `/tmp/liteapi-openapi-spec-diff-navigation-ui-smoke.png` and `/tmp/liteapi-openapi-local-drift-open-ui-smoke.png`.
- LiteAPI Computer UI smoke opened an isolated WebSocket request against a local `127.0.0.1:18786` server, verified the WS request-line badge and URL, opened the Body tab, saw selected JSON/text/binary message rows plus an unselected `skipped` row, clicked Send, displayed `101`, handshake headers, `x-websocket-messages-sent: 3`, and a three-event response array with `json hello`, `text note`, and `binary bytes`, showed all WebSocket assertions plus `websocket event array` passed in the Tests tab, and verified server logs received exactly three frames with no `do-not-send` payload.
- LiteAPI Computer UI smoke opened an isolated gRPC request against a local `127.0.0.1:18787` dynamic Greeter server, verified the gRPC server URL and `helloworld.Greeter/SayHello` request-line method fields, opened the Body tab, saw Load methods, Generate, method type `unary`, proto path, and a named message row, loaded the proto method list as `1 method`, generated a sample body with `"name": "string"`, clicked Send, displayed `200` with `{"message":"hello string"}`, showed response headers `grpc-status: 0`, `grpc-method: /helloworld.Greeter/SayHello`, and `x-grpc-smoke: liteapi-grpc`, showed all gRPC assertions plus `grpc response visible` passed in the Tests tab, and verified server logs received the generated request plus `x-request-id: grpc-ui-123` metadata.
- LiteAPI Computer UI smoke opened an isolated Unix-socket gRPC request against a local `/tmp/liteapi-grpc-ui-unix.sock` dynamic Greeter server, verified the request-line URL `unix:///tmp/liteapi-grpc-ui-unix.sock`, opened the Body tab, saw method type `unary`, proto path, and named `socket request` body `{"name":"Socket"}`, clicked Send, displayed `200` with `{"message":"hello Socket"}`, showed response headers `grpc-status: 0`, `grpc-method: /helloworld.Greeter/SayHello`, and `x-grpc-transport: unix`, showed all Unix transport assertions plus `grpc unix transport visible` passed in the Tests tab, and verified server logs received the request over Unix transport with `x-request-id: grpc-unix-ui` metadata.
- LiteAPI Wails dev browser smoke opened an isolated Collection > Protobuf tab, added `protos/greeter.proto` plus enabled import path `protos`, verified both rows showed `Found`, created a `Proto UI Smoke` gRPC request with no request-level proto path, opened Body, loaded collection-backed methods as `1 method`, selected `helloworld.Greeter/SayHello · unary`, clicked Generate, and verified the message body contained `"name": "string"`. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened an isolated Collection > Client Certificates tab, added a blank editable certificate row, filled `grpcs://127.0.0.1:55434`, PEM cert, and key paths through the rendered table, verified `state.json` persisted the row, created a `gRPC mTLS UI Smoke` request to the local TLS reflected gRPC service, set `grpc.testing.TestService/UnaryCall` through the request-line method input, disabled Verify TLS in Settings for the self-signed server, clicked Send, displayed `200` with `username: "ui-mtls"` and `serverId: "liteapi-ui-client"`, and verified fixture logs recorded `peer=liteapi-ui-client`. Direct native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke created an isolated `grpcurl UI Smoke` gRPC request, verified the `grpcurl` action was disabled before the request-line method was entered, filled `grpc://127.0.0.1:50051` plus `grpc.testing.TestService/UnaryCall`, clicked Generate grpcurl command, and verified the modal rendered `grpcurl -plaintext -d '{}' 127.0.0.1:50051 grpc.testing.TestService/UnaryCall`. Direct native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke created an isolated `gRPC Stream Timeline UI Smoke` bidirectional request against a local reflected gRPC service, filled `grpc://127.0.0.1:55435`, `grpc.testing.TestService/FullDuplexCall`, method type `bidi-streaming`, and two UI-entered message rows, clicked Send, displayed `200`, verified response headers `grpc-stream: bidi`, `grpc-request-count: 2`, and `grpc-response-count: 2`, expanded the Timeline row, and verified the detail rendered `CALL grpc.testing.TestService/FullDuplexCall grpc://127.0.0.1:55435 -> 200 (bidi stream, sent 2, received 2)`. The local fixture log confirmed two bidi messages were received. Direct native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke exercised Bruno-style live gRPC streaming controls against a local reflected bidi service: filled `grpc://127.0.0.1:55435`, `grpc.testing.TestService/FullDuplexCall`, method type `bidi-streaming`, and two message rows, clicked Start stream, verified `Streaming 1 events` and enabled per-row Send controls, sent rows `one`/`two`, verified the response event log rendered `sent json one` with `b25l` and `sent json two` with `dHdv`, clicked End, verified `Ended 7 events`, received response rows for both payloads, response headers `grpc-stream: bidi`, `grpc-request-count: 2`, `grpc-response-count: 2`, `grpc-status: 0`, `x-grpc-stream-connected: false`, `x-grpc-stream-ended: true`, and `x-grpc-stream-events: 7`, expanded Timeline detail `gRPC stream end ... sent 2 received 2`, and confirmed fixture logs saw `body="one"` and `body="two"`. The smoke also caught and fixed stale gRPC message editor state by moving message name/content persistence to `input` events. Direct native Computer capture remains blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke reran the live bidi flow after adding the receiver loop, clicked Start stream, sent `one`, and verified the response event log showed `received json response 1` with `body: "b25l"` while the stream still displayed `Streaming 3 events`; sent `two` and verified `received json response 2` with `body: "dHdv"` while the stream still displayed `Streaming 5 events`; response headers before End showed `grpc-request-count: 2`, `grpc-response-count: 2`, `x-grpc-stream-connected: true`, and `x-grpc-stream-ended: false`; after End the UI-written state showed final headers `grpc-status: 0`, `x-grpc-stream-connected: false`, `x-grpc-stream-ended: true`, `x-grpc-stream-events: 7`, persisted sent/received/end events, and Timeline rows for start, each message, and end with `sent 2 received 2`. Fixture logs confirmed `body="one"` and `body="two"`.
- LiteAPI Wails dev browser smoke verified Bruno-style gRPC response metadata/trailers and live Timeline taxonomy against a reflected bidi service that sent `x-ui-initial: metadata-ok` and trailer `x-ui-trailer: trailers-ok`: created a gRPC request through the UI, selected `bidi-streaming`, sent message `one` with `b25l`, clicked End, verified response tabs `Metadata 2` and `Trailers 1`, opened Metadata and Trailers tabs and saw `content-type: application/grpc`, `x-ui-initial: metadata-ok`, and `x-ui-trailer: trailers-ok`, opened Timeline and saw gRPC badges `request`, `message`, `response`, `end`, `metadata`, and `status`, verified the response row label `Response Message #1`, and expanded metadata/status rows to confirm metadata and trailer key/value detail tables. Fixture logs confirmed `body="one"` and stream end.
- LiteAPI Wails dev browser smoke verified gRPC WSSE auth metadata: opened an isolated `gRPC WSSE UI Smoke` request against a local reflected unary service, used the Auth tab to select `wsse` and fill username/password through the rendered controls, clicked Send, displayed the `grpc-wsse-ui`/`liteapi-wsse` response payload, and verified fixture logs received an `X-WSSE` UsernameToken containing `Username="grpc-ui-user"` with digest, nonce, and created timestamp. The smoke also caught and fixed stale Auth-tab username/password state by moving those fields to `input` events.
- LiteAPI Wails dev browser smoke verified authenticated gRPC reflection: opened an isolated `gRPC Reflection Auth UI Smoke` request against a local reflection service that rejects `ServerReflectionInfo` without `Authorization: Bearer reflection-ui-token`, used the Auth tab to enter a Bearer token, clicked Body > Load methods, verified the reflected method dropdown populated with `grpc.testing.TestService/UnaryCall` and no unauthenticated error, and confirmed fixture logs saw the bearer metadata on both reflection streams. The smoke also caught and fixed stale Bearer/static OAuth token field state by moving those request Auth fields to `input` events.
- LiteAPI Wails dev browser smoke verified normal top-Send gRPC Timeline taxonomy against a reflected bidi service: created a `gRPC Top Send Timeline UI Smoke` request, filled `grpc://127.0.0.1:55438`, `grpc.testing.TestService/FullDuplexCall`, method type `bidi-streaming`, and two UI-entered messages, clicked the main Send button, displayed `200`, verified Metadata `content-type` plus `x-grpc-initial: bidi-header`, verified Trailers `x-grpc-trailer: bidi-trailer`, opened Timeline, and confirmed the newest send group rendered the preserved aggregate request row plus gRPC event badges `request`, `message` x2, `response` x2, `metadata`, `status`, and `end`. The smoke expanded response rows and saw payloads `b25l`/`dHdv`, then expanded metadata/status rows and saw the metadata and trailer key/value detail tables.
- LiteAPI Wails dev browser smoke verified failed server-streaming gRPC metadata/trailer parity against a local reflected service: clicked the main Send button, displayed status `8`/`ResourceExhausted`, preserved the partial response body `cGFydGlhbA==`, showed Metadata `content-type`, `x-error-bin`, and repeated `x-error-initial`, showed Trailers with repeated `x-error-trailer` and binary trailer rows, opened Timeline, and confirmed the newest failed send rendered request, response, metadata, status, and error rows with no outbound message/end rows. The smoke expanded the response row and verified it showed the partial payload instead of `stream quota`, then expanded status/error rows and verified trailer detail tables.
- LiteAPI Wails dev browser smoke verified Bruno-style gRPC `User-Agent` handling against a local reflected unary service: created a gRPC request through the rendered request form, filled `grpc://127.0.0.1:55441`, `grpc.testing.TestService/UnaryCall`, request metadata rows `User-Agent: LiteAPI-UI-UA/9.9` and `x-ui-ua: metadata-visible`, clicked Send, displayed `200`, and verified the response body/fixture log showed `user-agent=LiteAPI-UI-UA/9.9 grpc-go/...` while `x-ui-ua` stayed ordinary metadata.
- LiteAPI Wails dev browser smoke opened the Environments screen, edited the selected `Development` environment through the UI, added `uiSecret`, marked it secret, verified the editable table showed the new secret row, and inspected the isolated app data files to confirm `secrets.json` stored `$01:` encrypted values while `state.json` retained only secret placeholders with empty values. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI native Computer UI smoke seeded a Bruno legacy `$01:` AES value in collection `secrets.json`, selected the `Development` environment, clicked Send on `GET http://127.0.0.1:18080/legacy?token={{token}}`, displayed `200` with `"token": "legacy secret"` and `"ok": true`, and the loopback verifier logged `TOKEN= legacy secret`; the companion backend fixture verifies `$00:` safeStorage imports hydrate as empty outside Electron.
- LiteAPI Wails dev browser smoke seeded an isolated workspace `.env` and collection `.env`, edited the request URL in the UI to use `{{process.env.LITEAPI_DOTENV_*}}`, clicked Send against a local `127.0.0.1:18791` echo server, and verified `200` with `priority: "collection"`, `workspace: "workspace-only"`, and `collection: "collection-only"` in the visible response, proving Bruno's collection `.env` > workspace `.env` > OS runtime precedence. Direct native Computer capture is still blocked by `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen, created a workspace global environment, verified it became active in the top global selector, added `ui_global_secret`, marked it secret, sent a request against local `127.0.0.1:18792` using `{{ui_global_secret}}`, and verified `200` plus visible `secret=from-global-ui`; file inspection confirmed `My Workspace/environments/Global.yml` and `state.json` keep the secret variable value empty while `secrets.json` stores the workspace environment secret as `$01:` encrypted data. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen `.env Files` card, created workspace `.env`, collection `.env`, and collection `.env.local` through the Table editor, verified only exact `.env` files showed as runtime files, deleted `.env.local` through the UI, sent a request against local `127.0.0.1:18793` using `{{process.env.LITEAPI_DOTENV_FILES_UI_PRIORITY}}`, and verified `200` plus visible `priority=collection`; file inspection confirmed workspace `.env` and collection `.env` were written and `.env.local` was removed. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen, created `Source JSON`, added plain `api_host` and secret `global_secret`, exported Bruno-compatible JSON with `info.type: bruno-environment` while confirming the secret value was blank, copied it as `Source JSON - Copy` with the secret value preserved in the editable copy, imported Bruno JSON as `Imported Runtime` with an imported secret value blanked, sent `GET {{api_host}}/runtime?value={{imported_value}}` against local `127.0.0.1:18794`, and verified `200` plus visible `query=value=from-import`; file inspection confirmed workspace YAML and `state.json` did not contain the secret strings while `secrets.json` held encrypted workspace secrets. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen, created duplicate `Team` global environments and verified Bruno-style conflict naming as `Team copy`, entered custom copy name `QA` and verified the copied global env became editable as `QA`, exported all globals as `single-file` and verified `Export: bruno-global-environments.json` plus a top-level `environments` array containing `Team`, `Team copy`, and `QA`, then exported all globals as `folder` and verified `Export: bruno-global-environments` plus separate-file manifest entries `Team.json`, `Team_copy.json`, and `QA.json`. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen, created `Disk Export`, added secret `disk_secret`, saved a single Bruno environment JSON file to `/tmp/liteapi-global-save-ui-5hv4VI/Disk_Export.json` through the explicit export path UI, verified the UI displayed the saved path, then switched to `folder` export and saved to `/tmp/liteapi-global-save-ui-5hv4VI/folder-export`; disk inspection confirmed both written Bruno JSON payloads had `info.type: bruno-environment`, retained the secret variable metadata, and scrubbed `hide-me-from-export`. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke seeded a legacy Bruno workspace with `workspace.yml activeEnvironmentUid` pointing to `Alpha.yml`, opened the app, verified the top global selector and Environments editor both selected `Alpha` and showed `mode=alpha`, then inspected disk to confirm `workspace.yml` no longer contained `activeEnvironmentUid` and `state.json` persisted `Alpha` as the active global environment across restarts. Direct native Computer capture was attempted first; Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen, verified the collection environment Variables tab showed `host` without leaking secret `token`, verified the Secrets tab showed `token` without leaking `host`, added `uiApiToken` through the Secrets tab and confirmed it stayed off Variables, then created a global environment, added `globalHost` on Variables and `globalToken` on Secrets, and confirmed the rows stayed isolated; disk inspection confirmed the global YAML scrubbed the secret value and `secrets.json` stored `$01:` encrypted values. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen, searched `host` and `token` across the active collection environment Variables/Secrets tabs, verified secrets never surfaced on Variables and variables never surfaced on Secrets, including the Bruno-style `No results found` empty state, then repeated the same scoped search behavior for a newly created global environment with `globalHost` and `globalToken`. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen, deleted `token` from the collection environment Secrets tab and verified the Variables tab still retained `host`, then created a global environment, deleted `globalToken` from Secrets, verified Variables still retained `globalHost`, and inspected disk to confirm `secrets.json` no longer contained environment secrets while the global YAML retained only the non-secret variable. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke edited a request URL to include `{{host}}`, secret `{{token}}`, and missing `{{missingVar}}`, selected the `Development` environment, verified the request variable inspector resolved `host` from Environment, showed `token` as masked with Environment scope, marked `missingVar` invalid, opened the token tooltip, revealed `secret-token`, copied it to the browser clipboard, then hid it again. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke edited a request URL to include `{{endpoint}}`, `{{process.env.HOME}}`, `{{createdFromTooltip}}`, and invalid `{{user id}}`, created `endpoint={{host}}/users` and `createdFromTooltip=created-value` directly from inspector tooltips, verified the request Vars tab showed the created values, copied the resolved nested endpoint value, edited `host` from the Environment-scoped tooltip and verified `endpoint` re-resolved to `https://api.changed.test/users`, confirmed `process.env.HOME` rendered as a read-only Process Env tooltip, confirmed invalid variable names show a warning and no editor, and inspected disk to confirm the created request variables were saved into `Echo JSON.yml`. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke seeded OS env plus workspace and collection `.env` files, edited a request URL to include `{{process.env.LITEAPI_TOOLTIP_UI_*}}`, verified Process Env tooltips show actual runtime values with Bruno precedence (`collection` over `workspace-only` over `os-only`) instead of a placeholder, copied the resolved `collection` value from the tooltip, and verified a missing process env renders as an empty read-only value. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke created request variable `authHeader=Bearer {{token}}` from the inspector tooltip while `token` was a selected-environment secret, verified `authHeader` displayed as masked even though it is a plain request variable, revealed `Bearer secret-token`, copied that resolved value, and verified the raw request variable stayed `Bearer {{token}}` in the Vars tab. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke created `lifecycleVar` from the inspector tooltip, saved `initial` with plain Enter, reopened the same popup and saved `initial-one` with plain Enter, edited again and clicked outside to blur-save `blur-saved`, and verified the final value persisted in the Vars tab. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke created `copyVarLatest` from the inspector tooltip, copied `first-copy` and verified the Copy button changed to `Copied`, disabled itself, then reverted to enabled `Copy`, edited the same tooltip to `latest-copy`, copied again, and verified the browser clipboard held the latest value while the Vars tab persisted `copyVarLatest=latest-copy`. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke edited the URL field to include `{{inlineVar}}` and invalid `{{user id}}`, verified Bruno-compatible inline `.cm-variable-invalid` spans, created `inlineVar=created-inline` from the inline `.CodeMirror-brunoVarInfo` popup, verified the URL token flipped to `.cm-variable-valid`, copied the inline value with `Copied` disabled/success feedback and timeout revert, verified the browser clipboard held `created-inline`, confirmed invalid names stay `.cm-variable-invalid` with the warning, and confirmed the request Vars tab persisted the created variable. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the JSON request body editor, entered fresh missing `{{bodyFreshKey}}` plus invalid `{{user id}}`, verified Bruno-compatible body `.cm-variable-invalid` spans, created `bodyFreshKey=fresh-secret-456` from the body `.CodeMirror-brunoVarInfo` popup, verified the body token flipped to `.cm-variable-valid`, copied the body value with `Copied` disabled/success feedback, verified the browser clipboard held `fresh-secret-456`, confirmed an outside click closed the active popup, then clicked `{{user id}}` and verified Bruno's warning-only invalid tooltip with no editable display. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke selected the `Development` environment, added `Authorization: Bearer {{token}}` in the request Headers table, verified the header value rendered a Bruno-compatible `.cm-variable-valid` span, opened the inline `.CodeMirror-brunoVarInfo` popup, verified the environment secret was masked as `********`, revealed `secret-token`, copied it with `Copied` disabled/success feedback, verified the browser clipboard held `secret-token`, and hid it again. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke added request query param `who={{paramFresh}}`, verified the param value rendered a Bruno-compatible `.cm-variable-invalid` span, created `paramFresh=param-created` from the inline `.CodeMirror-brunoVarInfo` popup, verified the param token flipped to `.cm-variable-valid`, copied it with `Copied` disabled/success feedback, verified the browser clipboard held `param-created`, and confirmed the request Vars tab persisted the created value. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke entered Bruno prompt variables in the URL, JSON body editor, and Params key/value table, verified valid prompt tokens like `{{?Prompt One}}`, `{{?Body Prompt}}`, and `{{?Param Prompt}}` render as `.cm-variable-prompt` without a tooltip wrapper, verified malformed prompt-looking tokens like `{{ ?Bad Prompt}}` and `{{? Bad Body}}` remain `.cm-variable-invalid`, verified normal `{{host}}` tokens remain `.cm-variable-valid`, and clicked prompt tokens to confirm no `.CodeMirror-brunoVarInfo` popup appears. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke edited the URL to include fresh path params `/:pathFresh...` and `/:pathOther...`, verified the URL overlay rendered them as `.cm-variable-invalid` while their Path values were empty, opened the Params tab and verified Bruno-style read-only Path rows were created with editable empty values, filled the rows and verified the URL tokens flipped to `.cm-variable-valid`, opened the inline `.CodeMirror-brunoVarInfo` popup and verified variable name, `Path Param` scope, editable value, and enabled Copy control, then edited the path value from the tooltip and verified it persisted back into the Path row while the URL token stayed valid. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened the Environments screen `.env Files` card, created workspace `.env` directly on disk, waited for the foreground refresh loop to show `.env` with `WATCHED=one` without pressing Reload, edited the file externally to `WATCHED=two` and verified the visible Table value updated automatically, then made a local unsaved edit `local-unsaved`, changed the disk file to `WATCHED=three`, and verified the dirty editor was not overwritten by the background refresh. Direct native Computer capture was attempted first, but `get_app_state` returned `cgWindowNotFound`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI UI smoke opened an isolated variable-precedence request against a local `127.0.0.1:18744` server with the `Development` environment selected, sent `GET /check`, resolved folder-over-environment `collision` to `folder`, resolved `{{process.env.LITEAPI_PROC_*}}` in URL/header/scripts, displayed `200`, and showed `res.status passed` plus `process env and precedence passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated prompt-variable request against a local `127.0.0.1:18745` server with the `Development` environment selected, clicked Send, rendered the `Input Required` dialog for URL/header/request/collection/environment/script prompt names, submitted values, displayed `200`, resolved nested prompt variables in the response URL/body/headers/scripts, and showed `res.status passed` plus `prompt variables interpolate passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated runner prompt-skip collection against a local `127.0.0.1:18746` server, clicked Run, skipped the first request because it contained `{{?Runner Name}}`, continued to a normal request, and rendered Runner totals `Total 2`, `Passed 1`, `Skipped 1` with the Bruno-style prompt-variable skip error in the first row.
- LiteAPI UI smoke opened an isolated runner runtime-variable collection against a local `127.0.0.1:18747` server, clicked Run collection in the Runner tab, persisted `bru.setVar("runner_token", "runner-secret")` from the first row into the collection runtime scope, interpolated `{{runner_token}}` into the second row header, rendered Runner totals `Total 2`, `Passed 2`, `Failed 0`, `Skipped 0`, displayed the server echo `"token": "runner-secret"`, and showed `runner runtime variable visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `node:buffer` request against a local `127.0.0.1:18749` server, clicked Send, executed pre-request `require("node:buffer")`, sent `X-Buffer-Token: YnJ1bm8tbm9kZS1idWZmZXI=`, displayed `200` plus the echoed token, and showed `node buffer alias visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `node:util` request against a local `127.0.0.1:18750` server, clicked Send, executed pre-request `require("node:util")`, sent `X-Util-Token: aGVsbG8gYnJ1bm8gNDI=`, displayed `200` plus the echoed token and `util.inspect` output, and showed `node util alias visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `node:url` request against a local `127.0.0.1:18751` server, clicked Send, executed pre-request `require("node:url")`, used `url.parse`, repeated query params, `url.resolve`, and `URLSearchParams`, sent `X-URL-Token: ZXhhbXBsZS5jb206MjpodHRwczovL2V4YW1wbGUuY29tL2M/eT0y` plus `X-URL-Params: a=1&b=two+words&a=2`, displayed `200`, and showed `node url alias visible passed` in the Tests tab.
- LiteAPI Wails browser UI smoke opened an isolated Developer FS collection against a local `127.0.0.1:18082` server, sent the default Safe Mode request and verified `require("fs")`/`require("node:fs")` were blocked with a visible `safe mode has no fs builtin passed` test and server echo `safeFs: "blocked"`, switched Collection > Overview to Developer Mode, sent a request that used `require("fs")`/`require("node:fs")` to read/write files outside the collection path via sync and promise APIs, displayed `200` with `fsValue: "outside:promise outside"`, persisted `developer mode fs builtin passed`, and left `securityConfig.jsSandboxMode` as `developer` in app state.
- LiteAPI UI smoke opened isolated `node:crypto` requests against local `127.0.0.1:18753` and `127.0.0.1:18768` servers, clicked Send, executed `require("node:crypto")`, used `createHash`, chained `update`, Buffer digest output, `createHmac`, `getHashes`, `getCiphers`, `pbkdf2Sync`, `scryptSync`, `timingSafeEqual`, and AES-CBC `createCipheriv`/`createDecipheriv`, displayed `200` with verifier echoes/failures `{}`, and showed `node crypto hash visible passed` plus `node crypto advanced visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated CommonJS circular request against a local `127.0.0.1:18754` server, clicked Send, executed local modules with simple circular `exports` mutation and reassigned `module.exports`, sent `X-CJS-Token: QS1CLUE=`, displayed `200`, and showed `CommonJS circular require visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated Developer Mode process-shim request against a local `127.0.0.1:18755` server, clicked Send, executed pre-request `process.nextTick`, sent `X-Process-Tick: tick-ok` and `X-Process-Env: ui-process-env`, displayed `200` with echoed process metadata, and showed `process shim visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `TextEncoder`/`TextDecoder` request against a local `127.0.0.1:18756` server, clicked Send, encoded `hé✓` into UTF-8 bytes, sent `X-Text-Bytes: 104,195,169,226,156,147`, displayed `200`, and showed `TextEncoder TextDecoder visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated Fetch API request against a local `127.0.0.1:18767` server, clicked Send, exercised `fetch`, `Request`, `Response`, `Headers`, `AbortController`, `FormData`, and `Blob`, performed a nested `POST /fetch`, displayed main response `200`, and showed `fetch globals visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated `node-fetch` request against a local `127.0.0.1:18774` server, clicked Send, executed `require("node-fetch")` and `require("node-fetch/commonjs")`, used default export plus `Headers`/`Request`/`Response`, performed a nested `POST /nested`, displayed `200` with `failures: {}`, and showed `node-fetch visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated EventTarget request against a local `127.0.0.1:18763` server, clicked Send, executed global `Event`, `CustomEvent`, and `EventTarget`, verified listener removal, `once`, custom detail, and cancelable default prevention through `X-Event-*` headers, displayed `200` with `failures: {}`, and showed `EventTarget globals visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated timer-globals request against a local `127.0.0.1:18764` server, clicked Send, executed awaitable `setTimeout`, `clearTimeout`, `setImmediate`, `clearImmediate`, `setInterval`, `clearInterval`, and `queueMicrotask`, verified the canceled timeout did not fire, displayed `200` with `failures: {}`, and showed `timer globals visible passed` in the Tests tab.
- LiteAPI UI smoke opened isolated global `crypto`/Web Crypto requests against local `127.0.0.1:18757` and `127.0.0.1:18769` servers, clicked Send, executed `globalThis.crypto` without `require`, sent a SHA-256 digest, random byte length, `randomUUID`, `crypto.subtle.digest`, and promise-returning `crypto.subtle.generateKey`, displayed `200` with verifier failures `{}`, and showed `global crypto visible passed` plus `web crypto visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `uuid` request against a local `127.0.0.1:18771` server, clicked Send, executed `require("uuid")`, exercised `v1/v3/v4/v5/v6/v7`, the Bruno regression pattern `const { v7: uuidv7 } = require("uuid")`, `v1ToV6`, `v6ToV1`, `validate`, `version`, `parse`, and `stringify`, sent `X-UUID-*` headers, displayed `200` with `failures: {}`, and showed `uuid expanded visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `crypto-js` request against a local `127.0.0.1:18758` server, clicked Send, executed Bruno fixture-style `CryptoJS.AES.encrypt(...).toString()` plus `AES.decrypt(...).toString(CryptoJS.enc.Utf8)`, sent SHA-256, HMAC, Base64, and decrypted plaintext headers, displayed `200`, and showed `crypto-js visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated `xml-formatter` request against a local `127.0.0.1:18775` server, clicked Send, executed `require("xml-formatter")`, verified the `default` export, pretty formatting, Bruno-style minify options, and invalid XML error handling through `X-XML-*` headers, displayed `200` with `failures: {}`, and showed `xml-formatter visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated `cheerio` request against a local `127.0.0.1:18778` server, clicked Send, executed `require("cheerio")`, verified `load`, selector lookup, `.text(...)`, `.addClass(...)`, `.attr(...)`, `$.html()`, and `default` export through request headers and Bruno fixture-style pre-request/post-response/tests, displayed `200`, and showed `cheerio visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated `xml2js` request against a local `127.0.0.1:18777` server, clicked Send, executed `require("xml2js")`, verified callback `parseString`, `Parser`, `parseStringPromise`, attributes, repeated child tags, default export, and invalid XML handling through `X-XML2JS-*` headers, displayed `200` with `failures: {}`, and showed `xml2js visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated `yaml` request against a local `127.0.0.1:18776` server, clicked Send, executed `require("yaml")`, verified parse/stringify/parseDocument/LineCounter/default export/node guards/invalid YAML handling through `X-YAML-*` headers, displayed `200` with `failures: {}`, and showed `yaml visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated Verify TLS request against a self-signed local HTTPS server at `https://127.0.0.1:18780/tls`, opened Settings, confirmed `Verify TLS` was checked, toggled it off, clicked Send, displayed `200` with `{"tls":"ok"}`, and showed `verify tls visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `moment` request against a local `127.0.0.1:18759` server, clicked Send, executed `require("moment")` and global `moment` UTC formatting, add/subtract arithmetic, Unix timestamp formatting, and duration math, displayed `200`, and showed `moment visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `chai` request against a local `127.0.0.1:18760` server, clicked Send, executed `require("chai")` from a pre-request script, verified module `expect`/`assert` identity with globals, sent `X-Chai-Module: chai-ok`, displayed `200`, and showed `chai require visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `querystring` request against a local `127.0.0.1:18761` server, clicked Send, executed `require("node:querystring")` and `require("querystring")`, parsed repeated keys, decoded plus-space/UTF-8 values, stringified arrays/nulls, sent `X-QS-*` headers, displayed `200` with `failures: {}`, and showed `querystring visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `node:os` request against a local `127.0.0.1:18762` server, clicked Send, executed `require("node:os")` and `require("os")`, read platform/arch/parallelism/CPU/path/EOL/load/signal helpers, sent `X-OS-*` headers, displayed `200` with `failures: {}`, and showed `node os visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `node:events` request against a local `127.0.0.1:18770` server, clicked Send, executed `require("node:events")` and `require("events")`, exercised EventEmitter listener ordering, `once`, static `once/on/listenerCount/getEventListeners/setMaxListeners/getMaxListeners` helpers, EventTarget listener inspection, sent `X-Events-*` headers, displayed `200` with `failures: {}`, and showed `node events visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `node:stream` request against a local `127.0.0.1:18765` server, clicked Send, executed `require("node:stream")` and `require("stream")`, exercised `Readable.from`, `Writable`, `Transform`, `Duplex`, and `pipeline` exports, sent `X-Stream-*` headers, displayed `200` with `failures: {}`, and showed `node stream visible passed` in the Tests tab.
- LiteAPI UI smoke opened an isolated `node:zlib` request against a local `127.0.0.1:18766` server, clicked Send, executed `require("node:zlib")` and `require("zlib")`, exercised synchronous gzip/gunzip, deflate/inflate, deflateRaw/inflateRaw, Brotli, unzip, compression levels, constants, and callback aliases through `X-Zlib-*` headers, displayed `200` with `failures: {}`, and showed `node zlib visible passed` in the Tests tab.
- LiteAPI Computer UI smoke opened an isolated `lodash` subpath request against a local `127.0.0.1:18773` server, clicked Send, executed `require("lodash/get")`, `require("lodash/cloneDeep.js")`, `require("lodash/groupBy.js")`, `require("lodash/sortBy")`, and `require("lodash/isEqual")`, sent `X-Lodash-*` verifier headers, displayed `200` with `failures: {}`, and showed `lodash subpath visible passed` in the Tests tab.
- Bruno reference UI was inspected through Computer: workspace overview, collection docs/settings tabs, request editor, response placeholder, and bottom status bar.
- Bruno reference UI smoke sent `GET https://jsonplaceholder.typicode.com/users` and displayed `200 OK`, timing, size, JSON format selector, and JSON code editor response.

## Browser UI Evidence

- LiteAPI Wails dev browser smoke opened an isolated `node:buffer` request against a local `127.0.0.1:18748` server, clicked Send, executed pre-request `require("node:buffer")`, sent `X-Buffer-Token: YnJ1bm8tbm9kZS1idWZmZXI=`, displayed `200` plus the echoed token, and showed `node buffer alias visible passed` in the Tests tab.
- LiteAPI Wails dev browser smoke opened an isolated `lodash` request against a local `127.0.0.1:18772` server, clicked Send, executed pre-request `require("lodash")`, `require("underscore")`, and global `_`, exercised `get`, `set`, `cloneDeep`, `chain`, and `groupBy`, displayed `200` with verifier failures `{}`, and showed `lodash visible passed` in the Tests tab. Direct Computer attach was attempted for this smoke, but macOS exposed zero LiteAPI windows in this run.
- LiteAPI Wails dev browser smoke opened an isolated URL Encoding request against a local `127.0.0.1:18779` verifier, opened Settings, confirmed `URL Encoding` started unchecked, toggled it on, clicked Send, displayed `200` with requested URL `http://127.0.0.1:18779/url%20path?name=John%20Doe&pre=john%2520doe`, and showed `url encoding visible passed` in the Tests tab. Direct native Computer capture for LiteAPI and Chrome was attempted first, but macOS returned `cgWindowNotFound`.
- LiteAPI Wails dev browser smoke opened an isolated binary WebSocket request against a local `127.0.0.1:18788` server, verified the Body tab row was selected with name `binary ping`, type `binary`, and content `cGluZw==`, clicked Send, displayed `101`, switched Response view to `Hex` and saw `00 01 fe ff 4f 4b`, showed headers `x-websocket-message-type: binary`, `x-websocket-message-base64: AAH+/09L`, `x-websocket-message-hex: 0001feff4f4b`, and `x-websocket-messages-sent: 1`, showed all WebSocket binary assertions plus `websocket binary metadata visible` passed in the Tests tab, and verified server logs received binary `70696e67` and sent `0001feff4f4b`. Direct native Computer capture for LiteAPI and Chrome returned `cgWindowNotFound`, and the in-app browser controller timed out before tab discovery, so this smoke used the same Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened an isolated live WebSocket request against a local `127.0.0.1:18080` echo server, verified the Body tab rendered `Disconnected`, `Connect`, `Send selected`, per-row `Send`, and disabled `Disconnect`, clicked `Connect` and saw `101`, `Connected`, and `0 events`, clicked the per-row `Send` twice plus top `Send selected` once, verified the response history grew to sent/received event pairs with `ack: {}` while the UI stayed `Connected`, clicked `Disconnect`, and verified `Disconnected`, disabled `Disconnect`, `7 events`, and a `system close disconnected` event. The local server log showed exactly three UI-sent WebSocket messages. Native Computer first listed LiteAPI but failed with `cgWindowNotFound`, so this smoke used the Wails browser endpoint.
- LiteAPI Wails dev browser smoke opened an isolated Manual Proxy GET request against local target/proxy servers at `127.0.0.1:18789` and `127.0.0.1:18790`, opened Collection > Proxy, verified manual mode, HTTP protocol, host `127.0.0.1`, port `18790`, auth enabled with `ui-proxy`/masked password, clicked Send, displayed `200` with `"proxied": true`, showed response header `X-Proxy-Smoke: true`, and showed `res.status passed`, `res.headers.X-Proxy-Smoke passed`, and `manual proxy visible passed` in the Tests tab. Server logs confirmed `Proxy-Authorization` and the proxy marker reached the target. Direct native Computer capture for LiteAPI returned `cgWindowNotFound`, and the in-app browser controller timed out before tab discovery, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened an isolated Client Cert GET request against a local mTLS server at `https://127.0.0.1:18791/mtls`, opened Collection > Client Certificates, verified domain `127.0.0.1:18791`, type `cert`, PEM cert/key paths, disabled PFX field, and passphrase field, clicked Send, displayed `200` with `"clientCert": true` and subject `liteapi-ui-client`, showed response header `X-Client-Cert-Smoke: true`, and showed `res.status passed`, `res.headers.X-Client-Cert-Smoke passed`, and `client certificate visible passed` in the Tests tab. Server logs confirmed the request arrived with client certificate subject `liteapi-ui-client`. Direct native Computer capture for LiteAPI was attempted first, but `get_app_state` returned `cgWindowNotFound` for `LiteAPI` and `com.wails.LiteAPI`, so this smoke used the Wails browser endpoint through Chrome DevTools.
- LiteAPI Wails dev browser smoke opened Collection > Presets, set request type to WebSocket and Base URL to `wss://preset.example/socket`, verified the sidebar new-request type selector adopted WebSocket, clicked the Create request `+`, and verified a new `CONNECT New Request` row plus an active WebSocket request editor with URL `wss://preset.example/socket`. Direct native Computer capture for LiteAPI was attempted first, but `get_app_state` returned `cgWindowNotFound` for `LiteAPI` and `com.wails.LiteAPI`, so this smoke used the Wails browser endpoint through Chrome DevTools.

## Blocked Verification

- URL Encoding native Computer smoke was attempted on 2026-07-02 after launching Wails dev with `/tmp/liteapi-url-encoding-smoke/state`; Computer listed `LiteAPI` and Chrome as running, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, the app bundle path, `Google Chrome`, and `com.google.Chrome`. The same Wails UI flow passed through the in-app browser controller.
- OAuth2 additional-parameter UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; Computer listed `LiteAPI` as running, but `get_app_state` failed with `cgWindowNotFound` for app name, bundle ID, and bundle path. Bruno reference window lookup also timed out for `Bruno` and `/Applications/Bruno.app`, while its bundle ID was ambiguous due a mounted Bruno volume.
- OAuth2 authorization-code UI smoke was attempted on 2026-07-03 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; Computer listed `LiteAPI` as running, but `get_app_state` failed with `cgWindowNotFound`, and macOS accessibility reported the `LiteAPI` process with no windows for both Wails dev and packaged app launches. The Wails browser-dev fallback rendered the Svelte UI and exposed the OAuth2 fields, but its browser-control action layer timed out on Send/checkbox interactions and Wails `BrowserOpenURL` did not open the local `/authorize` URL from that surface. Backend loopback auth-code coverage remains in `TestOAuth2AuthorizationCodeFetchesTokenWithLoopbackCallback`; native Computer UI evidence is still pending.
- OAuth2 implicit UI smoke was attempted on 2026-07-03 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app` and launching it with isolated `LITEAPI_DATA_DIR=/tmp/liteapi-oauth2-implicit-ui`; Computer listed/recently knew `LiteAPI`, and the packaged process was running at `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app/Contents/MacOS/LiteAPI`, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the app bundle path. Backend loopback implicit coverage remains in `TestOAuth2ImplicitFetchesTokenWithLoopbackFragmentCallback`; native Computer UI evidence is still pending.
- gRPC editor UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running at `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app/Contents/MacOS/LiteAPI`, but Computer `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Scoped env/global/collection script variable persistence UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running at `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app/Contents/MacOS/LiteAPI` and Computer listed `LiteAPI` as running, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Async JavaScript helper UI smoke for top-level `await`, async `test(...)`, awaitable `bru.sleep`, and awaitable/thenable `bru.sendRequest` was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` as running, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Nested `bru.runRequest` UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` as running, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Promise-style `bru.cookies.jar()` UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path, and rejected the executable path as an invalid app identifier.
- Safe-mode `setTimeout` script UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Script `btoa`/binary `atob` UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Script `tv4` UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Script `Ajv`/`ajv-formats` UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.
- Script `axios` UI smoke was attempted on 2026-06-30 after building `/Users/mou/Developer/Workspace/lite-api-doc/LiteAPI/build/bin/LiteAPI.app`; the LiteAPI process was running and Computer listed `LiteAPI` at bundle id `com.wails.LiteAPI`, but `get_app_state` failed with `cgWindowNotFound` for `LiteAPI`, `com.wails.LiteAPI`, and the full app path.

## Implemented And Tested

- Workspaces, per-workspace runtime Scratch collections, tabs, default sample data, and persistent app state with Bruno-style scratch collection/tab exclusion from persisted snapshots.
- Opening existing `.bru`/`bruno.json` and legacy `.yml` OpenCollection folders by path, refreshing them from disk, foreground-watching opened collection folders for external Bruno/OpenCollection tree-file changes with Bruno `ignore` path-prefix suppression, dirty-draft protection, and deleted-request tab cleanup, hydrating `collection.bru`/`opencollection.yml` headers/auth/variables/scripts/tests/docs plus `.bru`/`.yml` environment files, rendering nested folder labels from `folder.bru`/`folder.yml`, creating Bruno-style root and nested folders with sanitized filesystem names, duplicate/reserved-name validation, sibling sequence, and default inherited auth metadata, renaming folders by editing display metadata or moving the filesystem directory while updating nested folder/request state, applying folder-level headers/auth/variables, editing folder settings for Headers, Vars pre-request/post-response, Auth, Script, Test, and Docs with `folder.bru`/`folder.yml` save-back, creating and saving Bruno-style scratch/transient requests to a `bruno-scratch-*` temp collection directory, and saving regular request edits back to their original `.bru`/`.yml` files.
- Collection-level and folder-level headers/auth inheritance for request execution.
- Bruno-compatible collection presets: parse/save `brunoConfig.presets` and OpenCollection `config.presets`, normalize Bruno `ws` to LiteAPI WebSocket requests, apply preset request type and Base URL to new requests, and expose Collection > Presets in the UI.
- Bruno-compatible collection protobuf settings: parse/save `brunoConfig.protobuf` and OpenCollection `config.protobuf`, preserve proto file paths and enabled import paths without persisting Bruno's runtime `exists` flags, expose Collection > Protobuf in the UI, and use collection-level proto files/import paths as the gRPC descriptor source when a request has no proto path.
- Bruno-compatible Generate Documentation: Collection Overview exposes version/count summary and a Generate Documentation modal with environment selection, exports a standalone `<collection>-documentation.html` file that embeds OpenCollection YAML in Bruno's `collectionData` shell, loads the OpenCollection docs CDN assets, preserves Bruno-style folder/request sidebar ordering, and filters environments according to the modal selection.
- Bruno-compatible Share Collection: Collection Overview exposes Share Collection beside Generate Docs, with Bruno Collection (ZIP) selected by default and marked Recommended, Single File (YAML), and Postman cards; ZIP exports a Bruno/OpenCollection folder layout while excluding `.git` and `node_modules`, YAML exports bundled OpenCollection with environment secret values scrubbed, and Postman exports HTTP/GraphQL requests while warning that WebSocket/gRPC requests are skipped.
- Bruno-compatible response examples: parse repeated inline `.bru` `example { ... }` blocks, give loaded examples deterministic IDs from request path/index, save a live response as an inline example in the same request file, create blank/manual response examples with Bruno's suggested `example` naming, optional description, current request snapshot, and `200 OK` text response defaults, render saved examples in the response pane, open newly saved/cloned/created examples as tabs, restore response-example tabs into the Examples pane, close deleted example tabs, rename/clone/delete saved examples from the UI with Bruno-style `Name (Copy)` clone naming, edit saved example description, request method/URL/query-param sync/params/headers/body-mode/raw body with JSON prettify, form-url-encoded body rows, multipart body rows, and file body rows, edit-mode drag handles for reorderable request/response example rows, response status/status text/body type/body with JSON prettify/checkbox-free response headers, Content-Type header edits that sync body type, `.bru` persistence, and generate cURL/fetch snippets from saved example request snapshots.
- HTTP request execution with params, headers, JSON/text/XML/form/multipart/file bodies, Bruno-style Body tab form-url-encoded and multipart key/value editors with multiline value cells, multipart file-path/content-type columns, Bruno-style selected file-body table with add/remove/select rows and content-type auto-fill, `.bru` `body:form-urlencoded`, `body:multipart-form`, and multi-row `body:file` plus YAML file-body save-back, Basic/Bearer/API key auth, cookies, redirects, timeouts, Bruno-compatible request URL Encoding toggle with path/query/hash/pre-encoded query behavior, request TLS verification toggle for self-signed HTTPS targets, collection-level manual HTTP proxy execution with auth/bypass, Bruno-style app-level proxy preferences for Off/On/System Proxy/PAC plus collection inherit/enabled/disabled precedence, environment and macOS `scutil --proxy` system proxy resolution, PAC `FindProxyForURL` evaluation with common helper functions, collection-level PEM/PFX client certificate mTLS, response headers/body/base64/size/timing.
- Digest auth challenge retry is implemented, backend-tested, and Computer UI-smoked for Bruno-style username/password `auth:digest` request blocks, including `.bru` parse/write round trip and visible Digest mode/credentials execution against a local challenge server.
- AWS SigV4 direct credential signing is implemented, backend/build-tested, and Computer UI-smoked for Bruno-style `auth:awsv4` request blocks, including access key id, secret access key, session token, service, region, `.bru` parse/write round trip, YAML auth preservation, visible request/collection UI fields, and signed execution against a local verifier; shared-file static profile credential loading, `credential_process` profile loading, `source_profile` AssumeRole chains, `credential_source=Environment` AssumeRole chains, `web_identity_token_file` AssumeRoleWithWebIdentity chains, legacy SSO cached-token profiles, `sso-session` cached-token profiles, `sso-session` token refresh, and MFA AssumeRole profile chains with env/profile token-code providers are backend-tested, while interactive MFA token prompts and native smoke remain pending.
- WSSE UsernameToken auth is implemented, backend/build-tested, and Computer UI-smoked for Bruno-style `auth:wsse` username/password request blocks, including Bruno-compatible `X-WSSE` digest generation, `.bru` parse/write round trip, YAML auth preservation, visible request/collection UI fields, and execution against a local WSSE verifier.
- OAuth1 HMAC-SHA1/HMAC-SHA256/HMAC-SHA512, RSA-SHA1/RSA-SHA256/RSA-SHA512, and PLAINTEXT signing is implemented, backend/build-tested, and Computer UI-smoked for Bruno-style `auth:oauth1` request blocks, including header/query/body placement, form body signing, optional body hash, inline PEM and `@file(...)` private keys, `.bru` parse/write round trip, YAML auth preservation, request/collection UI fields, and HMAC-SHA1 header execution against a local verifier.
- NTLM auth is implemented, backend/build-tested, and Computer UI-smoked for Bruno-style `auth:ntlm` username/password/domain request blocks, including variable interpolation, HTTP challenge negotiation, request body replay across the handshake, `.bru` parse/write round trip, YAML auth preservation, visible request/collection UI fields, and execution against a local challenge verifier.
- OAuth2 client credentials, password grant, refresh-token reuse, authorization-code loopback, explicit hosted, and Bruno default-hosted browser token fetching, and implicit loopback, explicit hosted, and Bruno default-hosted fragment-token fetching are implemented and backend/build-tested for Bruno-style `auth:oauth2` request blocks, including Basic-auth-header versus body client credentials, access token versus ID token source, header versus URL token placement, in-process token cache and refresh-token reuse, encrypted OAuth2 credential-store persistence/hydration for `$oauth2.<credentialsId>.<field>` variables, Bruno-style credential variables plus `bru.resetOauth2Credential`, access-token and refresh-token additional parameter placement across headers/query/body, authorization-code/implicit browser grant field preservation (`callback_url`, `authorization_url`, `state`, `pkce`), authorization-code authorization URL generation with state/scope/PKCE and authorization-request query params, implicit authorization URL generation with `response_type=token`, scope/state/additional authorization query params, local, hosted, and `bruno://app/oauth2/callback`/`liteapi://app/oauth2/callback` protocol callback capture through Wails open-url and second-instance hooks without leaking implicit tokens into Timeline URLs, hosted `redirect_uri` preservation, Wails protocol registration, Bruno-style default in-app authorization modal plus persisted `Use system browser for OAuth` preference routing, callback/token Timeline rows, `.bru` parse/write round trip, YAML auth preservation, request/collection UI fields, and a client-credentials script-helper UI smoke; Electron-style embedded-window authorization request header/session interception parity and broader auth-mode Computer smoke remain pending.
- GraphQL-over-HTTP body execution.
- Persistent cookie manager core: capture `Set-Cookie` from final and redirect responses, store domain/path/expires/session/secure/httpOnly/sameSite/hostOnly metadata, attach matching cookies to future requests and redirect targets, reject foreign/public-suffix/IP-domain cookies like Bruno's `tough-cookie` jar, normalize invalid raw/script cookie paths to `/`, apply expired `Set-Cookie` values as deletes, persist cookie jar values encrypted in `state.json` and hydrate them at startup, list cookies in the app shell, delete individual cookies, and clear all cookies.
- Advanced cookie manager controls are implemented and backend/build-tested: structured add/edit form, raw `Set-Cookie` import, cookie search, domain grouping, and per-domain clear. Add/import/search/edit/grouping/per-domain clear, encrypted cookie persistence, and cookie validation are UI-smoked.
- Bruno-compatible WebSocket message storage and execution are implemented and backend/frontend/build-tested plus Computer/UI-smoked, including `.bru` and OpenCollection YAML named `body.ws` messages, selected-message queue execution, JSON/XML/text/binary message type preservation, WS request-line UI, message add/edit/remove controls, Bruno-style live Connect/Disconnect controls, top Send/per-row send over a persistent socket with auto-connect, collection manual proxy tunneling with browser UI smoke, collection client certificates for `wss://`, prompt-variable interpolation, `settings.keepAliveInterval` millisecond persistence plus ping frames, shutdown cleanup, sent/received/system response event history with structured sent/received rows, handshake metadata, response event-array capture for multi-message sends, binary response base64/hex metadata with Hex view rendering, and browser UI smoke coverage for editing/persisting the keep-alive setting and event-log rendering.
- SSE `text/event-stream` detection for HTTP responses.
- Bruno-compatible gRPC storage fidelity, editor fields, proto-backed unary execution, collection-level proto file/import-path fallback, server-reflection-backed unary execution, method discovery/sample body generation with request metadata/auth on reflection, grpcurl command generation, server/client/bidi streaming runtime, and Bruno-style live start/send/end/cancel controls with response-pane sent/received/system event history are implemented and backend/frontend/build-tested plus Computer/UI-smoked for `.bru` and OpenCollection YAML request files, including `metadata`, `protoPath`/`protoFilePath`, `methodType`, repeated request messages, generated Wails bindings, request-line RPC method editing, save-back to the original request path, dynamic `.proto` descriptor loading, reflection descriptor loading when no proto source is supplied, JSON request/response conversion, plaintext `grpc://`, TLS `grpcs://` with verify-toggle and collection client-certificate mTLS, and Unix-socket `unix://` targets, gRPC metadata headers including Bruno-compatible `*-bin` base64 decode on send plus OAuth2 static/client-credentials and WSSE UsernameToken auth metadata, separated gRPC response Metadata/Trailers tabs with counts, stream request/response count headers, expanded Timeline stream-count summaries, Bruno-style normal Send and live Timeline event rows for request/message/response/metadata/status/end/cancel/error with payload, metadata, and trailer details including non-OK streamed partial responses, JSON-array rendering for streamed responses, live bidi receive-before-End event-log rendering, and collection runner inclusion; advanced call credentials remain a gap.
- Collection runner with pass/fail/skipped counts, Bruno-style runner configuration panel request selection/select-all/reset/no-selection disabled run and inter-request delay, Bruno-style prompt-variable request skipping/reporting, shared collection runtime variables across rows, and Bruno-style `bru.setNextRequest(...)`, `bru.runner.setNextRequest(...)`, `bru.runner.skipRequest()`, and `bru.runner.stopExecution()` controls.
- Collection variables, collection runtime variables from `bru.setVar/deleteVar/deleteAllVars`, collection/folder/request post-response variables that merge by Bruno precedence and persist into runtime vars before post-response scripts/tests, editable folder pre-request/post-response variable settings with Bruno `folder.bru` save-back, `.bru`/`.yml` environments, workspace `.env` plus collection `.env` process-env precedence for `{{process.env.NAME}}` interpolation and `bru.getProcessEnv`, with global `process.env` reserved for Developer Mode, Bruno-style workspace/collection `.env Files` listing/create/edit/delete UI with Table/Raw editing, exact `.env` runtime marking, and foreground live refresh with dirty-editor protection, editable collection environments with Bruno-style Variables/Secrets tab separation, active-tab-scoped search, and delete isolation, request variable inspector tooltips with scope, invalid-variable, copy success/revert feedback, latest-value copy, and secret reveal controls, Bruno-compatible inline URL/body/header/param/form-url-encoded/multipart value variable spans and `.CodeMirror-brunoVarInfo` popups plus prompt-variable `.cm-variable-prompt` highlighting without popovers, Bruno-compatible URL path-param `/:name` highlighting, Path rows, and editable `Path Param` tooltips, workspace-level global environment CRUD/selection/copy with custom copy names and Bruno conflict naming, global environment Variables/Secrets tab separation, active-tab-scoped search, and delete isolation, Bruno JSON single-object/single-file/separate-file export payloads plus export-to-disk via native save dialog or explicit path, Bruno `workspace.yml activeEnvironmentUid` migration into per-workspace active state, Bruno/Postman environment JSON import, per-workspace active state, Bruno-shaped encrypted collection/global environment `secrets.json` storage with secret-value scrubbing from env files, exported JSON, imported Bruno secret values, and `state.json`, Bruno legacy `$01` AES secret hydration, Bruno-compatible empty hydration for `$00` safeStorage values when Electron safeStorage is unavailable, request variables, dynamic timestamps, secret flags, typed values, script-persisted scoped env/global/collection variable values, Bruno folder-over-environment precedence, bounded recursive interpolation inside scoped variable values, and Bruno-style prompt variable dialog values for request sends.
- Assertions, the existing `expect status equals 200` test DSL, and a minimal embedded JavaScript runtime for `script:pre-request`, `script:post-response`, `tests`, `bru.getVar/setVar/deleteVar/interpolate/sleep`, root runtime bulk helpers (`hasVar`, `getAllVars`, `deleteAllVars`), collection bulk delete, env/global/collection variable helper aliases with dirty typed env/global/collection/runtime persistence back to state, prompt variable values supplied by the request dialog, Bruno-style OAuth2 credential variable reads plus `bru.resetOauth2Credential`, `bru.getProcessEnv`, runtime metadata helpers (`cwd`, `getEnvName`, `getCollectionName`, `isSafeMode`), Safe Mode process-env access through `bru.getProcessEnv` and interpolation without a global `process` object, Developer Mode process globals (`process.version`, `process.versions.node`, `process.platform`, `process.arch`, `process.env`, `process.cwd`, `process.nextTick`, and `global.process`), `TextEncoder`/`TextDecoder` UTF-8 globals, Fetch API globals (`fetch`, `Request`, `Response`, `Headers`, `AbortController`, `FormData`, `Blob`) backed by the HTTP bridge plus `node-fetch`/`node-fetch/commonjs` module aliases, Event/CustomEvent/EventTarget globals, global `crypto` random/hash/HMAC/Web Crypto `subtle` promise helpers, `bru.utils.minifyJson/minifyXml`, core synchronous and awaitable `bru.sendRequest` HTTP calls with string/object configs, callback success/error paths, success thenables, and catchable awaited failures, top-level await scripts, awaitable `bru.sleep`, Promise/thenable `setTimeout` handles with fire-and-forget drain, clearable `setTimeout`/`setImmediate` and repeating `setInterval` handles that run until cleared, and `queueMicrotask`, async `test(...)` callbacks, nested awaitable `bru.runRequest` by request name/path with shared runtime variables/cookies and Bruno-style skipped WebSocket/gRPC targets, bounded safe `require` shims for `chai`, `jsonwebtoken`/`jwt`, `uuid` v1/v3/v4/v5/v6/v7/parse/stringify/validate helpers, `lodash`/`underscore` get/set/cloneDeep/isEqual/chain/collection helpers plus `lodash/<helper>` subpath imports, `nanoid`, `path`/`node:path` plus Developer Mode `path/posix`/`node:path/posix` and `path/win32`/`node:path/win32`, `url`/`node:url`, `URL`, `URLSearchParams`, `querystring`/`node:querystring` parse/stringify helpers, `os`/`node:os` platform/path/CPU/memory/user helpers, `events`/`node:events` EventEmitter helpers, `stream`/`node:stream` basic constructor/pipeline helpers, `zlib`/`node:zlib` synchronous gzip/deflate/Brotli helpers with constants and callback aliases, `crypto`/`node:crypto` random/hash/HMAC/KDF/timing-safe/AES-CBC helpers, `crypto-js` AES/hash/HMAC helpers, `xml-formatter` pretty/minify helpers, `cheerio` load/selector/text/class helpers, `xml2js` parseString/Parser helpers, `yaml` parse/stringify/parseDocument helpers, `moment` formatting/arithmetic/duration helpers, `buffer`/`node:buffer`, `util`/`node:util`, Developer Mode `util/types`/`node:util/types` module, Developer Mode `process`/`node:process` module, Developer Mode `console`/`node:console` module, Developer Mode `timers`/`node:timers` and `timers/promises`/`node:timers/promises` modules, Developer Mode `assert`/`node:assert` and `assert/strict`/`node:assert/strict` modules, Developer Mode `dns`/`node:dns` and `dns/promises`/`node:dns/promises` lookup/resolve modules, Developer Mode `http`/`node:http` and `https`/`node:https` client request/get modules, Developer Mode `fs`/`node:fs` filesystem read/write APIs with Safe Mode `fs` withheld, `atob`/`btoa`, and local CommonJS files inside the collection root with caching and circular dependency support plus Developer Mode package.json main, JSON modules, scoped packages, nested package dependencies, and collection node_modules package resolution, `bru.setNextRequest`, `bru.runner.setNextRequest/skipRequest/stopExecution`, current-request `bru.cookies` direct read/write/iteration access plus Promise/await and callback-style `bru.cookies.jar` cross-URL helpers, request URL/method/header/body mutation including Bruno-compatible form-url-encoded `req.setBody` object/array/string bodies, timeout and redirect controls, response JSON parsing toggle, Bruno request helpers (`getUrl`, `getHost`, `getPath`, `getQueryString`, `getMethod`, `getName`, `getTags`, `getAuthMode`, `getPathParams`, `getTimeout`, `setTimeout`, `setMaxRedirects`, `disableParsingResponseJson`, `getExecutionMode`, `getBody`, `getHeader`, `getHeaders`, `setHeader`, `setHeaders`, `deleteHeader`, `deleteHeaders`, `onFail`) plus direct `req.name/tags/timeout/pathParams`, mutable `req.headerList` read/write/iteration methods (`add`, `upsert`, `remove`, `clear`, `populate`, `repopulate`, `assimilate`, callback contexts, `toObject` options), callable `res(...)`/`res.jq(...)` JSON response selectors for common Bruno query forms, read-only `res.headerList`, `res.status/statusCode/body/json/data/dataBuffer/headers/url/responseTime`, Bruno response helpers (`getStatus`, `getStatusText`, `getHeader`, `getHeaders`, `getBody`, `setBody`, `getResponseTime`, `getUrl`, `getSize`, `getDataBuffer`), `test(...)`, common Chai-style `expect` helpers (`equal/eql/include/match/property/lengthOf/a/an/above/below/least/most`, chain aliases, `throw`, `jsonSchema` with Draft-07/format/strict/coerceTypes support, `jsonBody` with deep/path/bracket/quoted-key matching, and `true/false/null/undefined/ok/exist/empty/json` accessors), basic `assert`, and response-attached `console.log/debug/info/warn/error` forwarding.
- Collection-scoped JavaScript sandbox mode is implemented with Bruno-style app-local state: new/opened/imported collections default to Safe Mode, `Collection.securityConfig.jsSandboxMode` persists in LiteAPI state without writing `bruno.json`, refresh/open preserves the mode, `bru.isSafeMode()` reflects it, Safe Mode exposes the Bruno local promise-returning `setTimeout` wrapper while hiding global timer helpers, hiding global `process`, keeping `bru.getProcessEnv`, and withholding Developer Mode native-style modules such as `fs`/`node:fs`/`fs/promises`/`node:fs/promises`, `process`/`node:process`, `console`/`node:console`, `timers`/`node:timers`, `assert`/`node:assert`, and `dns`/`node:dns`; Developer Mode exposes `process` globals, `process` module APIs, clearable global timers/immediates/microtasks, `console` module APIs, `timers` module APIs, `assert` assertion APIs, `dns` lookup/resolve APIs, plus `fs`/`node:fs` and direct `fs/promises`/`node:fs/promises` read/write/promise filesystem APIs, and Collection > Overview has tested Safe Mode/Developer Mode controls.
- Bruno JSON, `.bru`, `collection.bru`, `.bru`/`.yml` environments, legacy OpenCollection `.yml`, Postman nested folder/request import with collection variables, collection/folder/request post-response variables/actions, collection/folder/request Basic/Bearer/API key/Digest/AWS SigV4/OAuth1/OAuth2 auth inheritance, Insomnia V4/V5 folder/request import with environments, Basic/Bearer auth, params, variable normalization, JSON/XML/text/URL-encoded/multipart/GraphQL bodies, collection/folder/request event script preservation plus common `pm.*` translation for variables/tests/expect/request headers/response status-body-headers/header PropertyList methods/read-only cookies, URL query/path params, raw JSON/XML/text, URL-encoded, multipart, and GraphQL bodies, saved-response examples, OpenAPI JSON/YAML operation import with first-tag and path folder grouping, `TRACE` operations and `.bru` round-trip, top-level webhooks, operation callbacks, `x-bruno-variants`, collection/path/operation server variables, path/query/header params, parameter defaults/examples/minimums, referenced and schema/example-derived request bodies, response examples with request-body pairing by example key and response header examples/defaults/refs, response-link post-response variable scripts, visible `apiKey` header/query rows, and basic `apiKey`/HTTP bearer/basic/digest/OAuth2 security mapping, export JSON, and `.bru`/`.yml` stringify/parse round trip.
- Git-backed collection metadata is backend/build-tested and Computer UI-smoked: connect/remove a collection remote with URL validation, persist `remote`, add/remove a managed `.gitignore` entry for in-workspace collections, preserve remotes across refresh/open, mark remote-backed missing folders as `notFoundLocally` ghost rows, render/remove ghost rows, and display Git badges/remotes.
- Git clone import flow is backend/build-tested and Computer/UI-smoked: detect `git --version`, show a dedicated Git-required modal when Git is missing, clone a remote repository into a chosen folder with Wails event-backed progress streaming, scan the clone or any local folder for Bruno collections, present candidates, and open selected collections into the workspace with their remote URL attached.
- Svelte workbench: request tabs, per-tab request/response pane persistence, persisted Bruno-style request/response orientation toggle, Bruno-style collapsible left sidebar and toggle button, saved response example tabs with save/create/clone/delete lifecycle sync and Examples-pane hydration, Bruno-style tab-management shortcuts including active-collection Save All Tabs and scoped/global reopen-last-closed-tab history, nested sidebar response-example rows/search, sidebar request search, Bruno-style global search modal with Bruno Documentation result, Bruno-style Preferences > Appearance theme mode and light/dark variant selectors, Bruno-style Preferences > Display code font/font size controls, zoom selector/persistence plus zoom shortcuts, Bruno-style Preferences > General SSL/custom CA/request timeout/store cookies/send cookies/default location/auto-save controls, Bruno-style Preferences > Cache File cache toggle/size/clear controls plus SSL session cache controls, Bruno-style Preferences > Keybindings grouped shortcut table with enabled/reset/custom shortcut controls, Bruno-style Preferences > OAuth2 Use system browser toggle, OAuth2 authorization modal with system-browser and callback fallback controls, Bruno-style notification trigger/modal with unread badge, All/Unread tabs, read/clear controls, Support links, Golden Edition licensing modal, Bruno-style Dev Tools surface with Console/Network/Performance/Terminal tabs, persisted bottom-drawer open state/active tab/drawer height/details panel width, Network method filters/sort/details with persisted sort and column widths, Performance CPU/process sampling selector/cards, PTY-backed terminal session create/list/input/resize/kill, Collection overview Open in Terminal, Reveal in Finder/Show in Folder, Clone, Rename, and Remove Collection actions, response tabs, response view modes, Bruno-style Timeline filter chips, OAuth2 token-request rows, and scripted `bru.sendRequest`/`bru.runRequest` request/pre-request rows with nested scripted-entry bubbling, environment screen, import/export screen, runner screen with configuration panel, Collection Share modal/export, Collection Generate Documentation modal/export, network log, parity ledger.

## Remaining Bruno Parity Gaps

- Full Bruno JS runtime: complete `bru`, `req`, `res`, Chai/JWT helpers, exact variable precedence, request failure controls, full event-loop Promise scheduling, remaining OAuth browser-flow/gRPC scripted-request timeline depth, broader safe/developer sandbox APIs and exact safe/developer error-mode differences, broader Node module compatibility, and exact sequential/sandwich preferences beyond the implemented default merge order.
- Full auth parity: interactive AWS MFA token prompts/native smoke, OAuth2 embedded-window authorization request-header/session interception parity, and remaining native Computer smoke for newer OAuth browser-flow paths.
- gRPC parity beyond the current runtime: advanced call credentials and any provider-specific credential edge cases not covered by metadata/auth/proto/reflection smokes.
- Import/export parity: full advanced Postman/Insomnia/OpenAPI/WSDL/OpenCollection conversion, broader auth/script translation such as callback-style cookie jars and OAuth2 token/current-token extras, advanced Postman `pm.*`/Chai translation, Insomnia file upload/body edge cases, and invalid schema reporting.
- OpenAPI sync: full Bruno tabbed Overview/Collection Changes/Spec Updates experience, dedicated split-pane `API Spec` tab with Swagger preview, virtualized large-spec diff rendering, richer conflict presentation, toolbar update badge polish, multi-source polish, and broader edge-case parity remain gaps; core OpenAPI 3.x fetch/cache/diff/connect/check/apply with persisted sync metadata, View spec cached/fallback content viewing, View Spec Diff current/updated line comparison plus Previous/Next navigation, connection settings, auto-check hash polling, preserved user values, endpoint decisions, compact review controls, and Collection Changes local drift reset/restore/delete/revert/Open actions is implemented and tested.
- Filesystem parity: full drag/drop folder tree operations, remaining response-example inner-row drag-gesture polish, and native chokidar-grade watcher refinements such as per-file reducer echoes and watcher-rebuild behavior after ignore-list edits.
- Environment/secrets parity: modal-level global environment manager polish, Electron safeStorage `$00` OS-keychain decryption outside Electron, and broader global env UI parity.
- Collection/settings parity: non-macOS OS proxy discovery, advanced PAC helper/cache parity, and remaining advanced settings surfaces.
- Git parity beyond Bruno's currently wired clone/remote surface: init/status/diff/stage/commit/branch/push/pull/fetch/conflict handling if those dormant helper APIs become user-visible parity targets.
- App shell parity: broader command palette surfaces beyond global search, xterm-grade terminal rendering/keyboard control, richer CPU history/graphs, remaining OAuth browser-flow/gRPC timeline detail parity, and AI sidebar.

## Verification Commands

```bash
go test ./...
cd frontend && npm run check && npm run build
$HOME/go/bin/wails build -nocolour
```

Recent focused OpenAPI Sync coverage: Bruno source inspection confirmed persisted `brunoConfig.openapi`, spec-cache metadata, OpenAPI 3.x-only validation, normalized method/path endpoint diffing, preserve-values merge semantics, review decisions `keep-mine`/`accept-incoming`, default accept-spec handling for added/updated/removed spec changes, Collection Changes local drift actions and Open buttons, Connection Settings, auto-check hash polling, `View spec` cached-first/source-fallback behavior, and `View Spec Diff` current/updated spec comparison with Previous/Next navigation. LiteAPI now passes `TestOpenAPISyncApplyPreservesUserValuesAndScripts`, `TestOpenAPISyncApplyHonorsEndpointDecisions`, `TestOpenAPISyncApplyCanKeepRemovedEndpoint`, `TestUpdateOpenAPISyncConfigPersistsAutoCheckSettings`, `TestCheckOpenAPIUpdatesComparesRemoteSpecHash`, `TestGetOpenAPISyncSpecReturnsCachedSpec`, `TestGetOpenAPISyncSpecFetchesWhenCacheMissing`, `TestGetOpenAPISyncSpecDiffComparesStoredAndIncomingSpec`, `TestGetOpenAPISyncSpecDiffFetchesConfiguredSource`, `TestOpenAPISyncFetchCacheAndRejectsSwagger`, `TestOpenAPILocalDriftDetectsAndAppliesCollectionChanges`, and `TestOpenAPILocalDriftIgnoresPreservedValues`, persists OpenAPI sync metadata through `bruno.json`/OpenCollection YAML/export, supports HTTP(S), local, `file:` and pasted-content sync sources, exposes Collection > OpenAPI Sync controls with compact review rows plus `Skip All`/`Accept All`, exposes Settings with auto-check intervals, runs lightweight update checks, shows synced specs through View spec, exposes View Spec Diff with current/updated line panes plus Previous/Next navigation, and exposes Collection Changes checks with Reset/Restore/Delete/Revert All/Open actions. The Wails browser UI smokes imported v1 with sync enabled, pasted v2, clicked Connect, Check Updates, and Apply Sync, verified added/updated/removed counts, verified a new endpoint appeared, exercised review decisions by keeping one updated endpoint, skipping one added endpoint, deleting one removed endpoint, confirmed the sidebar reflected those choices, checked local drift, saw `1 modified · 1 deleted · 0 added`, reverted all to spec, saw `0 modified · 0 deleted · 0 added`, opened Connection Settings, saved Disabled and Every 15 min auto-check states, verified `bruno.json` persisted the interval, opened View spec and verified stored source/content for `Spec Viewer Smoke API`, opened View Spec Diff and verified `1 added · 1 updated · 0 removed · 0 unchanged`, source path, old/new spec lines, `/owners`, and `page`, then verified Spec Diff Previous/Next navigation and Collection Changes Open returning to the drifted request editor.

Recent focused scripting sandbox/timer coverage: Bruno source inspection confirmed sandbox mode is app-local collection security state, defaults to Safe Mode, maps Safe Mode to QuickJS and Developer Mode to Node VM, exposes `bru.isSafeMode()`, gives Safe Mode a local promise-returning `setTimeout` wrapper, and exposes Node timer globals including repeating `setInterval` only in Developer Mode. LiteAPI now persists `Collection.securityConfig.jsSandboxMode` in app state without writing `bruno.json`, preserves it across collection refreshes, exposes Collection > Overview Safe Mode/Developer Mode controls, and passes `TestJavaScriptRuntimeSupportsSafeSetTimeout`, `TestJavaScriptRuntimeSupportsDeveloperTimerGlobals`, and `TestJavaScriptRuntimeSupportsRepeatingSetInterval`. A Wails browser UI smoke imported an isolated Bruno collection, verified the overview started in Safe Mode, clicked Developer Mode, sent a request whose pre-request script awaited a three-tick `setInterval`, displayed `200` with `"ticks": "3"`, persisted a passing `developer interval sandbox` test, clicked Safe Mode, sent a Safe Mode request using local `setTimeout`, displayed `200` with `"safeTimer": "yes"`, persisted a passing `safe timeout sandbox` test, and the local server logs confirmed `/developer interval=3` and `/safe safe=yes`. Broader safe/developer sandbox APIs, developer-mode native module loading, broader npm edge cases, and exact safe/developer sandbox error-mode differences remain runtime parity gaps.

Recent focused scripting sandbox/timers builtin coverage: Bruno source inspection confirmed Developer Mode NodeVM resolves Node builtin modules through native `require`, including `timers`, `node:timers`, `timers/promises`, and `node:timers/promises`, while Safe Mode QuickJS stays on bounded shims. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsTimersBuiltins`, exposing Developer Mode `timers`/`node:timers` callback handles plus `timers/promises`/`node:timers/promises` `setTimeout`, `setImmediate`, `setInterval` iterator, and `scheduler.wait/yield` helpers. The Wails browser UI smoke imported `Developer Timers Builtin`, switched Collection > Overview to Developer Mode, sent the request, displayed `200` with `callback: "callback,immediate"`, `interval: "2"`, `promises: "timeout|promise-immediate|promise-interval|promise-interval"`, and `scheduler: "wait-yield"`, then showed `developer timers builtin visible passed`; the local echo server confirmed the timer-derived headers reached `GET /main`.

Recent focused scripting sandbox/console builtin coverage: Bruno source inspection confirmed Developer Mode NodeVM resolves Node builtin modules through native `require`, including `console` and `node:console`, while Safe Mode QuickJS stays on bounded shims. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsConsoleBuiltin`, exposing Developer Mode `console`/`node:console` aliases that forward module `log`/`warn`/`error` into response script logs and provide a stream-writing `Console` constructor. The Wails browser UI smoke imported `Developer Console Builtin`, switched Collection > Overview to Developer Mode, sent the request, displayed `200` with `module: "function:function"` and `stream: "stream {\"ok\":true}"`, showed `developer console builtin visible passed`, and the response Console tab displayed `module pre {"ok":true}`, `module warn 7`, and `module test ["done"]`; the local echo server confirmed the console-derived headers reached `GET /main`.

Recent focused scripting sandbox/filesystem coverage: Bruno source inspection confirmed Safe Mode QuickJS does not expose Node builtin `fs`, while Developer Mode Node VM allows builtin modules including `fs` and keeps `globalThis.fs` absent unless required. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsFSBuiltin`, keeps Safe Mode `require("fs")` and `require("node:fs")` unavailable, and exposes Developer Mode `fs`/`node:fs` sync plus promise read/write/mkdir/rm/unlink APIs against trusted filesystem paths. The Wails browser UI smoke imported `Developer FS UI Smoke`, sent `Safe No FS` in default Safe Mode and saw `200`, `safeFs: "blocked"`, and `safe mode has no fs builtin passed`; then it switched Collection > Overview to Developer Mode, sent `Developer FS`, saw `200` with `fsValue: "outside:promise outside"`, saw `developer mode fs builtin passed`, confirmed files were written outside the collection, and confirmed `securityConfig.jsSandboxMode` persisted as `developer`.

Recent focused scripting sandbox/fs promises coverage: Bruno source inspection confirmed Developer Mode NodeVM resolves Node builtin modules through native `require`, including `fs/promises` and `node:fs/promises`, while Safe Mode QuickJS stays on bounded shims. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsFSPromisesBuiltin`, keeps Safe Mode `require("fs/promises")` and `require("node:fs/promises")` unavailable, and exposes Developer Mode direct `fs/promises` aliases that match `require("fs").promises` and support promise `readFile`, `writeFile`, `mkdir`, and `rm` from request scripts. The Wails browser UI smoke imported `FS Promises UI Smoke`, sent `Safe FS Promises` in default Safe Mode and saw `safe mode hides fs promises builtin passed`, switched Collection > Overview to Developer Mode, sent `Developer FS Promises`, displayed `200` with `identity: "true"`, `methods: "function:function:function:function"`, and `read: "ui promise alias"`, then showed `developer fs promises builtin visible passed`; the local echo server confirmed those headers reached `GET /developer`, and the temp output directory was removed by `fs.promises.rm`.

Recent focused scripting sandbox/path submodule coverage: Bruno source inspection confirmed Developer Mode NodeVM strips `node:` prefixes, checks `node:module.builtinModules`, and resolves builtin modules through native `require`, including `path/posix`, `node:path/posix`, `path/win32`, and `node:path/win32`; Bruno's Safe Mode QuickJS path shim only registers the root `path` shim. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsPathSubmodules`, keeps Safe Mode path submodule names unavailable, adds Developer Mode `path.posix`/`path.win32` properties, and exposes direct POSIX/Win32 submodule aliases with slash/backslash `join`, `normalize`, `basename`, `extname`, `relative`, `parse`, and `format` checks. The Wails browser UI smoke imported `Path Submodules UI Smoke`, sent `Safe Path Submodules` in default Safe Mode and saw `safe mode hides path submodules passed`, switched Collection > Overview to Developer Mode, sent `Developer Path Submodules`, displayed `200` with all identity and path checks true, then showed `developer path submodules visible passed`; the local echo server confirmed those headers reached `GET /developer`.

Recent focused scripting sandbox/stream promises coverage: Bruno source inspection confirmed Developer Mode NodeVM strips `node:` prefixes, checks `node:module.builtinModules`, and resolves builtin modules through native `require`, including `stream/promises` and `node:stream/promises`; Bruno's node-stream fixture is marked Developer Mode-only and covers the root `node:stream` module. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsStreamPromisesBuiltin`, keeps Safe Mode `require("stream/promises")` and `require("node:stream/promises")` unavailable, adds Developer Mode `stream.promises`, and exposes direct `stream/promises` aliases with promise `pipeline` and `finished` success/error behavior. The Wails browser UI smoke imported `Stream Promises UI Smoke`, sent `Safe Stream Promises` in default Safe Mode and saw `safe mode hides stream promises builtin passed`, switched Collection > Overview to Developer Mode, sent `Developer Stream Promises`, displayed `200` with `identity: "true:true:true:true:true:true"`, `pipeline: "AB:true"`, and `finished: "done:boom"`, then showed `developer stream promises visible passed`; the local echo server confirmed those headers reached `GET /developer`.

Recent focused scripting sandbox/util types coverage: Bruno source inspection confirmed Developer Mode NodeVM strips `node:` prefixes, checks `node:module.builtinModules`, and resolves builtin modules through native `require`, including `util/types` and `node:util/types`; Bruno's node-util fixture is marked Developer Mode-only. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsUtilTypesBuiltin`, keeps Safe Mode `require("util/types")` and `require("node:util/types")` unavailable, and exposes Developer Mode direct `util/types` aliases that match `require("util").types` with `isUint8Array`, `isArrayBuffer`, `isTypedArray`, `isDate`, `isRegExp`, `isNativeError`, and `isPromise` checks. The Wails browser UI smoke imported `Util Types UI Smoke`, sent `Safe Util Types` in default Safe Mode and saw `safe mode hides util types submodule passed`, switched Collection > Overview to Developer Mode, sent `Developer Util Types`, displayed `200` with `identity: "true"` and `checks: "true:true:true:true:true:true:true"`, then showed `developer util types submodule visible passed`; the local echo server confirmed those headers reached `GET /developer`.

Recent focused scripting sandbox/assert builtin coverage: Bruno source inspection confirmed Developer Mode NodeVM resolves Node builtin modules through native `require`, including `assert`, `node:assert`, `assert/strict`, and `node:assert/strict`, while Safe Mode QuickJS stays on bounded shims. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsAssertBuiltin`, exposing Developer Mode `assert`/`node:assert` and `assert/strict`/`node:assert/strict` aliases with `AssertionError`, `ok`, `equal`, `strictEqual`, `deepStrictEqual`, `notDeepStrictEqual`, `throws`/`doesNotThrow`, `match`, `rejects`/`doesNotReject`, and strict submodule behavior. The Wails browser UI smoke imported `Developer Assert Builtin`, switched Collection > Overview to Developer Mode, sent the request, displayed `200` with `module: "function"`, `strict: "function"`, and `failure: "ERR_ASSERTION:AssertionError"`, then showed `developer assert builtin visible passed`; the local echo server confirmed the assertion-derived headers reached `GET /main`.

Recent focused scripting sandbox/http builtin coverage: Bruno source inspection confirmed Developer Mode NodeVM resolves Node builtin modules through native `require`, including `http` and `https`, while Safe Mode QuickJS stays on bounded shims. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsHTTPBuiltins`, exposing Developer Mode `http`/`node:http` and `https`/`node:https` module aliases with client `request`/`get`, request header setters/getters/body writes, response status/statusMessage/headers/rawHeaders, and `data`/`end` events. The Wails browser UI smoke imported `Developer HTTP Builtin`, switched Collection > Overview to Developer Mode, sent the request, displayed `200` with `moduleBody: "ui-payload-tail"`, `moduleStatus: "200"`, `moduleReply: "reply-ok"`, `moduleComplete: "true"`, `moduleHeader: "ui-yes"`, and `moduleExtra: "ui-late"`, then showed `developer http builtin visible passed`; the local echo server confirmed the pre-request `POST /module?via=request`, main `GET /main`, and test-time `GET /module-get?via=get` paths.

Recent focused scripting sandbox/dns builtin coverage: Bruno source inspection confirmed Developer Mode NodeVM resolves Node builtin modules through native `require`, including `dns` and `dns/promises`, while Safe Mode QuickJS stays on bounded shims. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsDNSBuiltin`, exposing Developer Mode `dns`/`node:dns` plus `dns/promises`/`node:dns/promises` aliases with callback and promise `lookup`, `lookup(..., { all: true })`, `resolve`, `resolve4`, default result order helpers, and `Resolver` server-list helpers. The Wails browser UI smoke imported `Developer DNS Builtin`, switched Collection > Overview to Developer Mode, sent the request, displayed `200` with `lookup: "127.0.0.1:4"`, `resolve: "127.0.0.1"`, and `all: "2"`, then showed `developer dns builtin visible passed`; the local echo server confirmed the DNS-derived headers reached `GET /main`.

Recent focused scripting sandbox/process coverage: Bruno source inspection confirmed Safe Mode QuickJS exposes `bru.getProcessEnv` while Developer Mode Node VM exposes `process` and `global.process`. LiteAPI now passes `TestJavaScriptRuntimeSafeModeHidesProcessGlobal`, keeps Safe Mode free of global `process` while preserving `bru.getProcessEnv` and `{{process.env.NAME}}` interpolation, and keeps `process.version`, `process.env`, `process.cwd()`, `process.nextTick`, and `global.process` in Developer Mode. The Wails browser UI smoke imported `Process UI Smoke`, sent `Safe Process` in default Safe Mode and saw `200` with `"safeProcess": "undefined"`, `"safeGlobal": "undefined"`, `"safeEnv": "process-ui-ok"`, and `safe process hidden visible passed`; then it switched Collection > Overview to Developer Mode, sent `Developer Process`, saw `200` with `"tick": "tick-ok"`, `"env": "process-ui-ok"`, `"cwd": "/tmp/liteapi-process-ui-smoke"`, saw `developer process visible passed`, and confirmed `securityConfig.jsSandboxMode` persisted as `developer`.

Recent focused scripting sandbox/process builtin coverage: Bruno source inspection confirmed Developer Mode NodeVM resolves Node builtin modules through native `require`, including `process` and `node:process`, while Safe Mode QuickJS stays on bounded shims. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsProcessBuiltin`, exposing Developer Mode `process`/`node:process` aliases that match global `process`/`global.process`, preserve `process.env`, `process.cwd()`, `process.platform`, `process.arch`, and `process.nextTick`. The Wails browser UI smoke imported `Developer Process Builtin`, switched Collection > Overview to Developer Mode, sent the request, displayed `200` with `module: "function:function"`, `env: "process-builtin-ui-ok"`, `cwd: "/tmp/liteapi-process-builtin-ui-smoke.r7Rq0m"`, `platform: "darwin:arm64"`, and `tick: "module-tick"`, then showed `developer process builtin visible passed`; the local echo server confirmed the process-derived headers reached `GET /main`.

Recent focused scripting package-resolution coverage: Bruno source inspection confirmed Developer Mode Node VM resolves local package directories through `package.json#main`, parses JSON modules, prefers collection `node_modules`, supports scoped packages, and resolves nested package dependencies inside the VM context. LiteAPI now passes `TestJavaScriptRuntimeDeveloperModeSupportsPackageRequire`, supporting Developer Mode local package main files, `require("./pkg/data.json")`, bare package loads from collection `node_modules`, package subpaths such as `require("test-module/config")`, scoped packages such as `require("@scope/scoped")`, and nested package dependencies. The Wails browser UI smoke imported `Developer Package UI Smoke`, switched Collection > Overview to Developer Mode, sent `Developer Package`, displayed `200` with `packageToken: "7:nested-dep:helper:42:scoped:lib/main.js"`, showed `developer package require passed`, and persisted `securityConfig.jsSandboxMode` as `developer`.

Recent focused filesystem watcher coverage: Bruno source inspection confirmed `collection-watcher.js` chokidar setup, `add`/`addDir`/`change`/`unlink`/`unlinkDir` routing, `.env*`/`.git`/`node_modules` ignores, request/folder/root/environment parsing, relative-prefix `brunoConfig.ignore` matching, and reducer-side dirty draft protection for watcher echoes. LiteAPI now passes `TestRefreshChangedCollectionsReloadsExternalDiskChanges`, `TestRefreshChangedCollectionsSkipsDirtyDrafts`, `TestOpenCollectionHonorsBrunoConfigIgnore`, `TestOpenCollectionHonorsOpenCollectionBrunoIgnore`, and `TestRefreshChangedCollectionsHonorsBrunoConfigIgnore`, exposes a Wails-backed foreground collection folder poller, reloads external `.bru`/OpenCollection tree changes, adds/removes request rows, closes tabs for deleted request files, preserves runtime response state for unchanged requests, ignores `.env*` tree noise, honors `brunoConfig.ignore` and `extensions.bruno.ignore` path-prefix suppression for opened collections/environments/watcher fingerprints, and skips dirty draft collections rather than overwriting local edits. Wails browser UI smokes opened isolated Bruno collections, verified an external `ping.bru` edit updated the sidebar/editor to `Pong Smoke`, verified adding `extra.bru` surfaced `Extra Smoke`, edited the active URL into a draft, overwrote the disk file, and confirmed the UI kept the draft URL without showing the disk override; a separate ignore-path smoke opened a collection with `ignore: ["ignored"]`, verified hidden requests never rendered, changed/added ignored files without surfacing them, and verified a non-ignored request edit refreshed live to `Visible Final`. Native event-by-event chokidar semantics and watcher-rebuild behavior after ignore-list edits remain refinements.

Recent focused Sidebar Request Generate Code coverage: Bruno source inspection confirmed `Generate Code` appears only for non-folder `http-request` and `graphql-request` rows, uses menu id `generate-code` and generated test id `collection-item-menu-generate-code`, opens only when saved or draft request URL is non-empty and otherwise raises `URL is required`, titles the modal `Generate Code`, defaults preferences to Shell/curl with interpolation enabled, uses draft/current request data before saved request data, and resolves inherited auth before generation. LiteAPI now exposes `collection-item-menu-generate-code` on HTTP/GraphQL request rows, omits it for gRPC rows, and passes `TestGenerateRequestCodeUsesCurrentRequestAndVariables` plus `TestGenerateRequestCodeSupportsGraphQLAndRejectsInvalidTargets`, with a Wails browser UI smoke that generated cURL/fetch snippets from a current draft HTTP request with selected-environment variables, inherited collection headers, path/query/body interpolation, disabled row omission, and the Bruno modal title.

Recent focused Sidebar Item Reveal/Open Terminal coverage: Bruno source inspection confirmed every rendered folder/request/app item receives `show-in-folder` with test id `collection-item-menu-show-in-folder`, macOS/Windows/Linux labels from `getRevealInFolderLabel()`, `showInFolder(item.pathname)` IPC to Electron `shell.showItemInFolder`, console logging plus `toast.error('Error opening the folder')` on failure, and no success toast; folders additionally receive `open-terminal` with test id `collection-item-menu-open-terminal`, label `Open in Terminal`, and `openDevtoolsAndSwitchToTerminal(item.pathname || collectionPathname)`, while requests do not. LiteAPI now passes `TestRevealCollectionItemActionsUseFolderAndRequestPaths` and `TestRevealCollectionItemActionsRejectMissingTargets`, plus a Wails browser UI smoke that clicked folder/request `collection-item-menu-show-in-folder`, verified the macOS `Reveal in Finder` label, confirmed requests have no terminal action, clicked folder `collection-item-menu-open-terminal`, and verified the Terminal session cwd uses the physical folder path even when the folder display name differs.

Recent focused Sidebar Item Info coverage: Bruno source inspection confirmed the folder/request row `Info` menu item id/test id, shared `Info` modal title, close-only footer-hidden modal, `modal-close-button`, and two-row content: folders show `Folder Name` plus `Folder Name (on filesystem)`, requests show `Request Name` plus `File Name (on filesystem)`, with no type/path/uid/seq/created/updated fields and no copy behavior. LiteAPI now passes a Wails browser UI smoke that clicked folder and request `collection-item-menu-info`, verified both modal variants and omitted metadata, confirmed no submit footer, and closed through `modal-close-button`.

Recent focused Delete Request coverage: Bruno source inspection confirmed the request row `Delete` menu item id/test id, `Delete Request` modal title/body/buttons, `renderer:delete-item` request-file unlink plus `deleteRequestUid`, sibling request/folder resequence through `renderer:resequence-items`, request-tab-only close behavior that leaves response-example tabs open, missing-file error `The file does not exist`, and no success toast. LiteAPI now passes `TestDeleteRequestYAMLRemovesFileResequencesAndClosesOnlyRequestTab`, `TestDeleteRequestBruRemovesFileAndKeepsSiblingsResequenced`, and `TestDeleteRequestRejectsMissingFileAndNotFoundLocally`, plus a Wails browser UI smoke that clicked request-row `collection-item-menu-delete`, verified Bruno modal copy, clicked `modal-submit-btn`, and confirmed state/tab/disk/no-toast output.

Recent focused Rename Request coverage: Bruno source inspection confirmed the request row `Rename` menu item id/test id, `Rename Request` modal title, `#collection-item-name`, optional filesystem-name editing through `rename-request-edit-icon` and `#file-name`, extension suffix, validation, duplicate checking only when the filesystem name changes, draft save before rename, `renderer:rename-item-name`/`renderer:rename-item-filename`, request UID/example preservation, no resequence, and success toast `Item renamed successfully`. LiteAPI now passes `TestRenameRequestYAMLUpdatesNameKeepsFilePathAndTab`, `TestRenameRequestBruMovesFilePreservesIDExamplesAndTab`, and `TestRenameRequestRejectsDuplicateReservedInvalidMissingAndNotFoundLocally`, plus a Wails browser UI smoke that clicked request-row `collection-item-menu-rename`, verified modal controls, submitted `rename-item-button`, and confirmed state/tab/disk output.

Recent focused Clone Request coverage: Bruno source inspection confirmed the request row `Clone` menu item id/test id, shared `Clone Request` modal title, `#collection-item-name`, optional editable `#file-name` with extension suffix, default `<name> copy`/`sanitize(name) copy` values, `clone-item-button`, sibling request filename duplicate validation, `renderer:new-request` write path, request-only next `seq`, fresh request/example identities, queued cloned-tab open, and success toast `Request cloned!`. LiteAPI now passes `TestCloneRequestYAMLCopiesRequestOpensTabAndWritesUniqueFile`, `TestCloneRequestBruRefreshesExampleIDsAndPreservesExamples`, and `TestCloneRequestRejectsDuplicateReservedInvalidMissingAndNotFoundLocally`, plus a Wails browser UI smoke that clicked request-row `collection-item-menu-clone`, verified modal defaults and filesystem override, submitted `clone-item-button`, and confirmed cloned state/sidebar/tab/disk output.

Recent focused Clone Folder coverage: Bruno source inspection confirmed the folder row `Clone` menu item id/test id, `Clone Folder` modal title, `#collection-item-name`, optional editable `#file-name`, default `<name> copy`/`sanitize(name) copy` values, `clone-item-button`, duplicate/reserved filename validation, recursive serialized folder/request copy via `renderer:clone-folder`, root-only next `seq`, no tab auto-open, success toast `Request cloned!`, and current WebSocket skip behavior. LiteAPI now passes `TestCloneFolderYAMLCopiesNestedFoldersRequestsAndFreshIDs`, `TestCloneFolderBruCopiesExamplesAndSkipsWebSocketLikeBruno`, and `TestCloneFolderRejectsDuplicateReservedInvalidMissingAndNotFoundLocally`, plus a Wails browser UI smoke that clicked `collection-item-menu-clone`, verified the modal defaults and filesystem override, submitted `clone-item-button`, and confirmed recursive state/sidebar/disk clone output.

Recent focused Delete Folder coverage: Bruno source inspection confirmed the folder row `Delete` menu item id/test id, `Delete Folder` modal copy/buttons, recursive `fs.rmSync` disk deletion, request UID cleanup, parent sibling resequence via `renderer:resequence-items`, and request-tab-only close behavior that leaves response-example tabs untouched. LiteAPI now passes `TestDeleteFolderYAMLRemovesDirectoryNestedItemsAndRequestTabs`, `TestDeleteFolderBruRemovesFolderAndKeepsSiblingsResequenced`, and `TestDeleteFolderRejectsMissingNotFoundLocallyAndMissingDirectory`, plus a Wails browser UI smoke that clicked `collection-item-menu-delete`, verified Bruno modal copy, clicked `modal-submit-btn`, and confirmed recursive state/sidebar/disk removal.

Recent focused runner coverage: Bruno source/test inspection confirmed the runner configuration panel defaults enabled requests to selected and exposes counter, select-all, reset, delay, per-request rows, and disabled Run state when nothing is selected. LiteAPI now exposes the same visible controls and passes `TestRunCollectionWithOptionsSelectsRequestsAndAppliesDelay` plus a Wails browser UI smoke that selected only rows 2 and 3, verified `2 of 3 selected`, ran only `/second` and `/third`, measured a 52 ms gap after entering a 25 ms delay, verified select-all/deselect-all/reset, and confirmed Run disabled at `0 of 3 selected`.

Recent focused Generate Documentation coverage: Bruno source/test inspection confirmed the Collection Settings > Overview Generate Documentation flow, modal title/heading/test IDs, collection version/count summary, environment selection, and standalone OpenCollection HTML payload with `https://cdn.opencollection.com/docs.css` and `docs.js`. LiteAPI now passes `TestGenerateCollectionDocsBuildsBrunoStyleHTMLAndFiltersEnvironments` and a Wails browser UI smoke that opened a five-request/two-folder/three-environment collection, verified `v1.0.0` plus `2 Folders • 5 requests`, unchecked `Development`, observed the Select All indeterminate state and `(2/3 selected)`, downloaded `GenerateDocsOrder-documentation.html`, decoded the embedded YAML, verified Bruno-style folder/request ordering, and confirmed only Production/Staging environments were exported.

Recent focused Share Collection coverage: Bruno source inspection confirmed Share appears beside Generate Docs in collection menus/settings, the modal title `Share Collection`, default `Bruno Collection (ZIP)` selected with `Recommended`, `Single File (YAML)`, `Postman`, `Proceed`, ZIP save dialog behavior, `.git`/`node_modules` ZIP exclusion, and Postman WebSocket/gRPC warning. LiteAPI now passes `TestShareCollectionExportsYamlZipPostmanAndSaves` plus a Wails browser UI smoke that opened an isolated HTTP/WebSocket/gRPC collection, verified the Share modal default ZIP card/recommended badge, downloaded `Share UI Exact.zip`, selected Postman and verified the WebSocket/gRPC warning before downloading JSON that included only the HTTP request, selected YAML and downloaded bundled OpenCollection, and confirmed the secret environment value was scrubbed.

Recent focused gRPC coverage: `TestGrpcBruRoundTripPreservesMetadataProtoPathMethodTypeAndMessages`, `TestRequestDocsRoundTripForWebSocketAndGRPCBru`, `TestOpenGrpcBruCollectionAndSavePreservesGrpcBlocks`, `TestGrpcYAMLRoundTrip`, `TestGRPCUnaryRequestExecutesWithProtoAndMetadata`, `TestGRPCUnaryRequestCanUseServerReflection` with separated metadata/trailers and `*-bin` decode assertions, `TestGRPCUnaryRequestUsesUserAgentHeaderAsDialUserAgent`, `TestGRPCUnaryRequestAppliesOAuth2AndWSSEMetadata`, `TestGRPCReflectionUsesRequestMetadataAndAuth`, `TestGRPCUnaryRequestUsesCollectionClientCertificate`, `TestGenerateGrpcurlCommandIncludesMetadataProtoAndBody`, `TestGenerateGrpcurlCommandUsesCollectionClientCertificate`, `TestGenerateGrpcurlCommandUsesHeredocForClientStreaming`, `TestGRPCServerStreamingRequestReturnsJSONMessages`, `TestGRPCServerStreamingErrorPreservesMetadataTrailersAndPartialResponses`, `TestGRPCClientStreamingRequestSendsAllMessages`, `TestGRPCBidiStreamingRequestSendsAndReceivesMessages`, `TestGRPCStreamingTimelineCapturesMethodAndCounts` with normal Send event-type Timeline assertions, `TestGRPCLiveBidiStreamSendEndRecordsEvents` with live event-type Timeline assertions, `TestGRPCLiveStreamCancelMarksSessionCancelled`, `TestGRPCMethodsFromProtoGenerateSampleMessage`, `TestGRPCMethodsFromReflectionGenerateSampleMessage`, `TestCollectionProtobufFallbackLoadsImportedMethodsAndSamples`, `TestGRPCUnaryRequestUsesCollectionProtobufConfig`, the Wails browser UI smoke for Collection > Protobuf-backed gRPC method generation, the Wails browser UI smoke for `grpcs://` collection client-certificate mTLS, the Wails browser UI smoke for gRPC grpcurl command generation, the Wails browser UI smoke for gRPC bidi stream count headers plus expanded Timeline summary, the Wails browser UI smoke for normal top-Send gRPC request/message/response/metadata/status/end Timeline rows with expanded payload/metadata/trailer details, the Wails browser UI smoke for failed server-streaming gRPC metadata/trailer tabs plus request/response/metadata/status/error Timeline rows with partial-response payload detail, the Wails browser UI smoke for gRPC User-Agent channel-option handling, the Wails browser UI smoke for live gRPC start/send/end controls, sent/received event log, headers, expanded Timeline detail, and fixture payload logs, the Wails browser UI smoke for bidi receive-before-End behavior while the stream stayed connected, the Wails browser UI smoke for gRPC Metadata/Trailers tabs plus live request/message/response/metadata/status/end Timeline rows, the Wails browser UI smoke for gRPC WSSE auth metadata, and the Wails browser UI smoke for authenticated gRPC reflection method loading.

Recent focused WebSocket coverage: `TestWebSocketRequestSendsAndReadsMessage`, `TestWebSocketRequestSendsMultipleSelectedMessages`, `TestWebSocketBinaryResponsePreservesBase64AndHex`, `TestWebSocketBinaryEventArrayIncludesBase64AndHex`, `TestWebSocketMessagesBruAndYAMLRoundTrip`, and `TestRequestDocsRoundTripForWebSocketAndGRPCBru`.

Recent focused URL Encoding and TLS settings coverage: `TestEncodeRequestURLMatchesBrunoToggleBehavior`, `TestHTTPEncodeURLSettingControlsExecutionURL`, `TestHTTPVerifyTLSSettingAllowsSelfSignedServer`, `TestRequestSettingsEncodeURLRoundTrip`, and `TestOpenLegacyYMLCollectionFromDiskAndSaveRequest`.

Recent focused response example coverage: `TestGenerateResponseExampleCodeUsesRequestSnapshot`, `TestGenerateResponseExampleCodeBodyModes`, `TestGenerateResponseExampleCodeFindsSavedBruExample`, `TestOpenResponseExampleTabCreatesTabForSavedExample`, `TestCreateResponseExampleWritesInlineBruExampleAndOpensTab`, augmented `TestResponseExampleRenameCloneDeleteWritesInlineBruExamples` tab lifecycle assertions, plus Browser UI smokes for saved response example Generate Code, Open tab, nested sidebar example rows, sidebar search by example name, request-param row reordering, request-param bulk editing inside the example editor, save/clone/delete response-example tab lifecycle, native Computer smoke for manual response-example creation/open-tab hydration, native Computer smoke for response-example edit-mode drag handles/checkbox-free response headers, and native Computer smoke for response example Content-Type body-type sync.

Recent focused app-shell tab-state coverage: `TestUpdateOpenTabPanesPersistsPaneSelection` plus native Computer UI smoke for selecting Body/Headers on a normal request tab, restarting Wails, and verifying the tab restored to the same request and response panes.

Recent focused scratch/transient coverage: `TestWorkspaceScratchCollectionIsTransientAndFileBacked` plus native Computer UI smoke for selecting Scratch, creating `Scratch Smoke`, verifying `temp draft` and `Save temp`, saving the temp request, sending it to a loopback endpoint, and confirming the request file was written only under the transient `bruno-scratch-*` directory while persisted state excluded scratch collection/tab data.

Recent focused app-shell theme coverage: `TestPreferencesThemeModeAndVariantsPersist` plus native Computer UI smoke for Preferences > Appearance Light/Dark/System mode switching, all Bruno theme variant cards, live light/dark/Nord styling changes, and persisted `themeVariantDark: nord`.

Recent focused app-shell display coverage: `TestPreferencesDisplayZoomPersistAndNormalize`, `TestPreferencesFontPersistAndNormalize`, Wails browser UI smoke for Preferences > Display zoom options/defaults/live CSS zoom/persistence/reload plus zoom shortcuts, and Wails browser UI smoke for Code Editor Font/Font Size defaults, custom `Courier New`/`18px` live editor variables, `1..32` clamp, and reload persistence.

Recent focused app-shell General/Cache coverage: Bruno source inspection confirmed `request.customCaCertificate.enabled/filePath`, `request.keepDefaultCaCertificates.enabled`, `request.storeCookies`, `request.sendCookies`, `request.timeout`, `general.defaultLocation`, `autoSave.enabled/interval`, `cache.sslSession.enabled`, and `cache.file.enabled` defaults/runtime use. LiteAPI now persists the same shaped keys while mirroring legacy flat fields, applies custom CA roots and keep-default behavior to HTTP/WebSocket/gRPC TLS, splits cookie send/store behavior, applies global request timeout overrides across request protocols, uses `general.defaultLocation` for new collection paths, schedules delayed request auto-save from the Svelte workbench, exposes Preferences > Cache File cache size/clear/toggle plus SSL session cache clear/toggle, and uses the File cache for opened collection parse snapshots with content-hash invalidation. Backend evidence: `TestPreferencesGeneralRequestAutoSaveCachePersistAndNormalize`, `TestHTTPCustomCaCertificateAllowsSelfSignedServer`, `TestSSLSessionCacheEnablesTLSResumption`, `TestPreferencesStoreAndSendCookiesAreSeparate`, `TestPreferencesRequestTimeoutOverridesHTTPRequest`, and `TestCollectionFileCacheCachesInvalidatesAndClears`; UI evidence: Wails browser smoke for Preferences > General and Cache toggles/inputs/persistence plus File cache toggle/size/clear.

Recent focused app-shell keybinding coverage: `TestPreferencesKeybindingsPersistAndNormalize`, `TestPreferencesLayoutPersistAndNormalize`, `TestOpenTabManagementPersistsOrderAndActiveState`, `TestSaveAllTabsWritesOpenRequestTabs`, `TestSaveAllTabsAssignsUniquePathsForDuplicateRequestNames`, native Computer UI smoke for Preferences > Keybindings grouped defaults, custom Global Search recording to `Cmd+Shift+K`, persisted `keyBindings.globalSearch.mac`, runtime Global Search execution through the customized shortcut, and disabled-keybindings suppression, plus Wails browser UI smoke for `Cmd+N`, `Cmd+2`, `Cmd+9`, `Cmd+[`/`Cmd+]`, `Cmd+W`, `Cmd+Shift+W`, `Cmd+Shift+T`, `Cmd+Shift+S`, `Cmd+J`, response layout toggle click, persisted orientation restart, `Cmd+\`, sidebar toggle button, `Cmd+O`, and `Cmd+E`.

Recent focused app-shell notification coverage: `TestNotificationsMarkReadAndClearPersist` plus native Computer UI smoke for cookie-generated notifications, topbar unread badge, Notifications modal All/Unread tabs, automatic read-on-open, Unread empty state, Clear all, and persisted empty `notifications` state.

Recent focused app-shell support/licensing coverage: native Computer UI smoke for rail Support and Golden Edition entry points, Support modal and Preferences Support link list with Bruno's Documentation/Report Issues/Discord/GitHub/Twitter labels, Golden Edition Individuals and Organizations pricing tabs including Buy, and Global Search `Bruno Documentation` result/filtering.

Recent focused app-shell DevTools coverage: `TestDevToolsSnapshotReportsRuntimeAndStateCounts`, `TestCalculateCPUPercent`, `TestSendRequestInterpolatesExecutesAndRecordsResponse`, `TestPreferencesDevToolsPersistAndNormalize`, `TestTerminalSessionLifecycleRunsShellCommand`, plus native Computer UI smokes for Bruno reference Terminal and Performance tab shape, Dev Tools entry points, Console/Network/Performance/Terminal tabs, Network method filters with `Hide All`/`Show All`, Network columns `Method`/`Status`/`Domain`/`Path`/`Time`/`Duration`/`Size`, status sort ascending/descending, Request Details tabs for Request/Response/Network data, persisted DevTools open Network state and custom column widths after restart, runtime resource cards with CPU/process sampling, Terminal session empty states, and PTY-backed Terminal session create/input/output/close, plus Wails browser UI smoke for Dev Tools bottom-drawer resize and persisted drawer-height restoration.

Recent focused collection proxy coverage: `TestCollectionManualProxyExecutesHTTPRequest`, `TestCollectionManualProxyBypassSkipsProxy`, `TestCollectionProxyInheritUsesGlobalManualProxy`, `TestCollectionProxyInheritUsesSystemEnvironmentProxy`, `TestGlobalProxyOffDisablesEnvironmentProxy`, `TestCollectionManualProxyOverridesGlobalProxy`, `TestPACProxyRoutesFromFileAndFallsBackDirect`, `TestPACProxyEvaluatesFindProxyForURLLogic`, `TestMacOSScutilProxyOutputResolvesProxyAndBypass`, `TestCollectionProxyMetadataRoundTrip`, the Wails browser UI smoke for Collection > Proxy manual HTTP proxy execution, the Wails browser UI smoke for Preferences PAC proxy inheritance through Collection > Proxy `Inherit`, and the Wails browser UI smoke for PAC `FindProxyForURL` conditional routing.

Recent focused collection client certificate coverage: `TestCollectionClientCertificateExecutesMTLSRequest`, `TestCollectionClientCertificateMetadataRoundTrip`, `TestUpdateCollectionClientCertificatesPreservesBlankEditorRows`, `TestGRPCUnaryRequestUsesCollectionClientCertificate`, the Wails browser UI smoke for Collection > Client Certificates PEM HTTP mTLS execution, and the Wails browser UI smoke for `grpcs://` collection client-certificate mTLS.

Recent focused collection presets coverage: `TestCollectionPresetsApplyToNewRequests`, `TestCollectionPresetsMetadataRoundTrip`, and the Wails browser UI smoke for Collection > Presets applying WebSocket/Base URL defaults to a new request.

Recent focused collection protobuf coverage: `TestCollectionProtobufMetadataRoundTrip`, `TestCollectionProtobufFallbackLoadsImportedMethodsAndSamples`, `TestGRPCUnaryRequestUsesCollectionProtobufConfig`, and the Wails browser UI smoke for Collection > Protobuf-backed gRPC method generation.

Recent focused response example coverage: `TestBruResponseExamplesRoundTrip`, `TestBruResponseExampleFormURLEncodedRoundTrip`, `TestBruResponseExampleMultipartRoundTrip`, `TestBruResponseExampleFileBodyRoundTrip`, `TestSaveResponseExampleWritesInlineBruExample`, `TestSaveResponseExamplePreservesFormURLEncodedRows`, `TestSaveResponseExamplePreservesMultipartRows`, `TestSaveResponseExamplePreservesFileBodyRows`, `TestResponseExampleRenameCloneDeleteWritesInlineBruExamples`, `TestUpdateResponseExampleEditsInlineBruExample`, `TestGenerateResponseExampleCodeUsesRequestSnapshot`, `TestGenerateResponseExampleCodeBodyModes`, `TestGenerateResponseExampleCodeFindsSavedBruExample`, the UI smoke for saving a response example, the Wails browser UI smoke for response example rename/clone/delete controls, the Wails browser UI smoke for response example status/status-text/body/header editing, the Wails browser UI smoke for response example request method/URL/params/headers/body-mode/raw-body editing, the Wails browser UI smoke for response example URL query-param sync, the Wails browser UI smoke for response example description editing, the Wails browser UI smoke for response example request/response JSON prettify, the Wails browser UI smoke for response example form-url-encoded body rows, the Wails browser UI smoke for response example multipart body rows, the Wails browser UI smoke for response example file body rows, the native Computer UI smoke for response example Content-Type body-type sync, and the Wails browser UI smoke for response example cURL/fetch Generate Code after attempting native Computer first.

Recent focused response timeline coverage: `TestTimelineCapturesPreRequestSendRequest`, `TestTimelineCapturesRunRequestAndBubbledSendRequest`, `TestTimelineCapturesSkippedRunRequestTargets`, `TestTimelineCapturesOAuth2TokenRequest`, `TestOAuth2AuthorizationCodeFetchesTokenWithLoopbackCallback`, `TestOAuth2AuthorizationCodeSupportsHostedCallbackBridge`, `TestOAuth2AuthorizationCodeUsesHostedDefaultCallbackAndProtocolHandoff`, `TestOAuth2ImplicitFetchesTokenWithLoopbackFragmentCallback`, `TestOAuth2ImplicitSupportsHostedCallbackBridge`, `TestOAuth2ImplicitUsesHostedDefaultCallbackAndProtocolHandoff`, `TestGRPCStreamingTimelineCapturesMethodAndCounts`, `TestGRPCServerStreamingErrorPreservesMetadataTrailersAndPartialResponses`, `TestGRPCLiveBidiStreamSendEndRecordsEvents`, plus native Computer UI smokes for scripted `bru.sendRequest`, nested `bru.runRequest`, and OAuth2 token-request Timeline filter chips, request/pre-request/OAuth counts, newest-first request/runRequest/sendRequest/OAuth rows, Pre-Request and OAuth filtering, expansion, source-file detail, skipped runRequest target rows, the Wails browser UI smoke for gRPC bidi stream count expansion, the Wails browser UI smoke for normal top-Send gRPC request/message/response/metadata/status/end Timeline rows with expanded payload/metadata/trailer details, the Wails browser UI smoke for failed server-streaming gRPC request/response/metadata/status/error Timeline rows with partial-response payload and trailer details, the Wails browser UI smoke for live gRPC start/send/end Timeline summaries, and the Wails browser UI smoke for live gRPC request/message/response/metadata/status/end Timeline rows with expanded metadata/trailer details.

Recent focused app-shell search coverage: the Wails browser UI smoke for Bruno-style Global Search with `Cmd+K`, toolbar open, collection/request results, ArrowUp/ArrowDown navigation, Enter selection, no-match state, and Escape close.

Recent focused environment secret coverage: `TestCollectionEnvironmentSecretsStoreRoundTrip`, `TestCollectionEnvironmentSecretsHydrateBrunoEncryptedFallbacks`, `TestCollectionEnvironmentSecretHydratesForRequestExecution`, the native Computer UI smoke for Bruno legacy `$01` secret hydration in request sends, the Wails browser UI smoke for encrypted environment secret editing, the Wails browser UI smoke for collection/global Variables/Secrets tab separation, the Wails browser UI smoke for active-tab-scoped Variables/Secrets search, the Wails browser UI smoke for Variables/Secrets delete isolation, and the Wails browser UI smoke for request variable inspector secret reveal/copy.

Recent focused variable tooltip coverage: the Wails browser UI smoke for request variable inspector chips resolving environment values, masking/revealing/copying secret values, showing Bruno-style copy success/revert state with latest-value copy, masking non-secret variables that resolve through secret references, marking undefined variables invalid, creating/editing request variables from tooltips with request auto-save, plain-Enter save, repeated same-popup edits, blur-save on outside click, recursively resolving nested references after scoped edits, rendering process-env values read-only with collection/workspace/OS `.env` precedence and copy support, warning on invalid variable names, Bruno-compatible inline URL variable spans/popups with edit, copy, success/revert, invalid-name, and Vars-tab persistence checks, Bruno-compatible inline body editor variable spans/popups with missing-to-valid creation, copy success, outside-click close, and warning-only invalid-name checks, Bruno-compatible inline request header value variable spans/popups with environment secret masking, reveal/hide, copy success, and clipboard checks, Bruno-compatible inline query-param value variable spans/popups with missing-to-valid creation, copy success, and Vars-tab persistence checks, Bruno-compatible prompt-token `.cm-variable-prompt` highlighting without `.CodeMirror-brunoVarInfo` popovers in URL, body, and key/value table editors, Bruno-compatible inline form-url-encoded and multipart body value spans/popups with multiline value persistence and missing-to-valid creation, plus Bruno-compatible URL path-param `/:name` invalid/valid highlighting, read-only Path rows, and editable `Path Param` inline popups.

Recent focused request body coverage: `TestBruFormURLEncodedBodyRoundTrip`, `TestBruMultipartBodyRoundTrip`, `TestMultipartBodyContentTypeAndFilePathInterpolation`, `TestBruFileBodyRoundTrip`, `TestBruMultiFileBodyRoundTrip`, `TestYAMLFileBodyRoundTrip`, `TestFileBodyContentTypeAndPathInterpolation`, `TestFileBodyUsesSelectedRowAndCollectionRelativePath`, `TestFileBodyWithoutSelectedRowSendsNoBody`, and Wails browser UI smokes for form-url-encoded, multipart, file body editor, and file body table add/select/remove behavior.

Recent focused cookie coverage: `TestCookiePersistenceEncryptsValuesAndHydrates`, `TestCookieRuntimeValidationRejectsForeignPublicSuffixAndNormalizesPaths`, `TestJavaScriptRuntimeCookieJarRejectsInvalidDomainsAndNormalizesPaths`, the Wails browser UI smoke for encrypted cookie value persistence from the Cookies view, and the Wails browser UI smoke for cookie validation from the Cookies view.

Recent focused Git coverage: `TestGitRemoteMetadataManagedIgnoreAndGhostRows`, `TestGitCloneScanAndOpenSelectedCollections`, `TestGitVersionReportsMissingGit`, the Computer UI smoke for Git connect/remove, ghost rows, clone/open selected, and the Wails browser UI smoke for Git clone progress streaming.

Recent focused `.env` runtime/UI coverage: `TestDotEnvProcessEnvPrecedenceAcrossCollectionWorkspaceAndOS`, `TestProcessEnvInterpolationAndFolderEnvPrecedence`, `TestDotEnvFileManagerListsSavesDeletesAndRuntimeExactEnvOnly`, the Wails browser UI smoke for collection/workspace/OS process-env precedence, the Wails browser UI smoke for `.env Files` Table editor create/delete/runtime behavior, and the Wails browser UI smoke for foreground `.env` file live refresh with dirty-editor protection.

Recent focused global environment coverage: `TestWorkspaceGlobalEnvironmentSelectionPrecedenceSecretsAndDiskRoundTrip`, `TestWorkspaceActiveGlobalEnvironmentUIDMigratesFromWorkspaceYML`, `TestGlobalEnvironmentCopyImportExportScrubsSecretsAndRoundTrips`, `TestGlobalEnvironmentMultiExportAndCopyNameUsesBrunoConflictStyle`, `TestSaveGlobalEnvironmentExportWritesSingleFileAndFolder`, the Wails browser UI smoke for workspace global environment CRUD/selection, the Wails browser UI smoke for Bruno JSON global environment export/copy/import plus imported runtime-variable execution, the Wails browser UI smoke for custom copy names plus all-global single-file/separate-file export previews, the Wails browser UI smoke for global environment export-to-disk single-file and folder saves, the Wails browser UI smoke for `workspace.yml activeEnvironmentUid` migration, the Wails browser UI smoke for global Variables/Secrets tab separation, the Wails browser UI smoke for global active-tab-scoped Variables/Secrets search, and the Wails browser UI smoke for global Variables/Secrets delete isolation.
