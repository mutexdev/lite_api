openapi: 3.0.3
info:
  title: OpenAPI Fixture
  version: 1.0.0
servers:
  - url: http://127.0.0.1:18487
paths:
  /json:
    get:
      operationId: getFixtureJSON
      tags:
        - Fixture
      responses:
        "200":
          description: Fixture response
  /graphql:
    post:
      operationId: postFixtureGraphQL
      tags:
        - Fixture
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Fixture response
