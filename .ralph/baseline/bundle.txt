# Bundle-size baseline
# Captured on branch ralph/postman-switchable-v2, working tree at Phase 0 start.
# Command: cd frontend && npm run build
# Referenced by: US-036 (initial chunk must drop below 500 KB), improvement_v2.md §2.

date          2026-07-25
vite          8.1.0 (rolldown)
svelte        5.56.4

file                                    bytes      gzip
frontend/dist/assets/index-BsBUEQ9r.js  1159933    352560
frontend/dist/assets/index-LydZFBwi.css  113999     20490
frontend/dist/index.html                    370       260

# Single JS chunk — no code splitting configured. Vite emits its own warning:
#   "(!) Some chunks are larger than 500 kB after minification."
#
# US-036 target: initial chunk < 500,000 bytes.


# ===========================================================================
# US-036 — after splitting CodeMirror off the cold-launch path
# ===========================================================================
# Command: cd frontend && npm run build

file                                     bytes     note
frontend/dist/assets/index-*.js         533898    initial chunk (was 1159933)
frontend/dist/assets/codemirror-*.js    623453    deferred; loads on first
                                                  Body/Script/Tests/Docs open
frontend/dist/assets/CodeEditor-*.js      9759    deferred
frontend/dist/assets/CodeEditor-*.css      684    deferred
frontend/dist/assets/index-*.css        113609

# Initial JS: 1,159,933 -> 533,898 bytes. A 54% reduction, and 623 KB of
# CodeMirror now never loads for a user who does not open an editor.
#
# ---------------------------------------------------------------------------
# THE <500 KB CRITERION IS NOT MET, AND IS BLOCKED ON US-025.
#
# 533,898 is still 33,898 bytes over the story's 500,000-byte target. The
# remaining chunk is NOT vendor code: frontend/package.json "dependencies"
# contains ONLY @codemirror/* and codemirror, all of which are now deferred.
# What is left is App.svelte's own compiled output (~14k lines) plus the Svelte
# runtime, so there is nothing further to split out by dependency.
#
# Getting under 500 KB therefore requires splitting App.svelte itself, which
# means extracting the DevTools, Runner, Import, Preferences and OpenAPI-sync
# surfaces into components that can be dynamically imported. That is US-025
# ("Extract inlined modals from App.svelte"), which has not been done.
#
# US-036's own criterion "DevTools, Runner, Import, Preferences, OpenAPI-sync
# and CodeMirror are dynamically imported" has the same dependency: only
# CodeMirror is currently a separate component. The story does not state this
# ordering constraint; it should be run AFTER US-025.
# ---------------------------------------------------------------------------


# ===========================================================================
# Re-measured after US-025 (all 29 dialogs moved onto the shared Modal shell)
# ===========================================================================
#
#   initial chunk  533,898 -> 532,602 bytes   (-1,296)
#
# Essentially unchanged, and that is the expected result rather than a
# disappointment: US-025 replaced each dialog's hand-rolled backdrop/dialog
# WRAPPER with <Modal>, but every dialog's BODY is still inline in App.svelte.
# The wrappers were a few lines each; the bodies are the bytes.
#
# WHAT IT WOULD TAKE to clear the 500,000-byte target, measured rather than
# estimated. The 29 modal bodies total ~57 KB of source in App.svelte:
#
#   3,757  generate-docs          3,649  rename-request      3,613  clone-request
#   3,606  share-collection       3,454  rename-folder       3,350  clone-folder
#   3,337  new-folder             3,336  openapi-spec-diff   3,281  clone-collection
#   2,696  notifications          2,385  openapi-settings    ... 18 more under 2 KB
#
# The remaining gap is 32,602 bytes of MINIFIED output. Moving the bodies into
# lazily-imported components under lib/modals/ would very likely clear it, since
# ~57 KB of source minifies to more than that — but each extraction has to
# thread that dialog's form state and handlers as props, and the large ones
# (the folder/request/collection forms) carry a lot of both. This is a
# component-by-component job, not a scripted one: unlike the wrapper conversion,
# there is no structural pattern a script can derive the props from.
#
# So US-036 remains PARTIAL. The 54% reduction from deferring CodeMirror stands;
# the <500 KB criterion needs the modal bodies extracted, which is the natural
# continuation of US-025 rather than a separate technique.
