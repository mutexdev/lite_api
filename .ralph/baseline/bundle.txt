# Bundle-size baseline
# Captured on branch ralph/postman-switchable-v2, working tree at Phase 0 start.
# Command: cd frontend && npm run build
# Referenced by: US-036 (initial chunk must drop below 500 KB), improvement_v2.md §2.

date          2026-07-25
vite          8.1.0 (rolldown)
svelte        5.56.4

file                                    bytes      gzip
frontend/dist/assets/index-BsBUEQ9r.js  1159933    352560
frontend/dist/assets/index-LydZFBwi.css  113999     20490
frontend/dist/index.html                    370       260

# Single JS chunk — no code splitting configured. Vite emits its own warning:
#   "(!) Some chunks are larger than 500 kB after minification."
#
# US-036 target: initial chunk < 500,000 bytes.


# ===========================================================================
# US-036 — after splitting CodeMirror off the cold-launch path
# ===========================================================================
# Command: cd frontend && npm run build

file                                     bytes     note
frontend/dist/assets/index-*.js         533898    initial chunk (was 1159933)
frontend/dist/assets/codemirror-*.js    623453    deferred; loads on first
                                                  Body/Script/Tests/Docs open
frontend/dist/assets/CodeEditor-*.js      9759    deferred
frontend/dist/assets/CodeEditor-*.css      684    deferred
frontend/dist/assets/index-*.css        113609

# Initial JS: 1,159,933 -> 533,898 bytes. A 54% reduction, and 623 KB of
# CodeMirror now never loads for a user who does not open an editor.
#
# ---------------------------------------------------------------------------
# THE <500 KB CRITERION IS NOT MET, AND IS BLOCKED ON US-025.
#
# 533,898 is still 33,898 bytes over the story's 500,000-byte target. The
# remaining chunk is NOT vendor code: frontend/package.json "dependencies"
# contains ONLY @codemirror/* and codemirror, all of which are now deferred.
# What is left is App.svelte's own compiled output (~14k lines) plus the Svelte
# runtime, so there is nothing further to split out by dependency.
#
# Getting under 500 KB therefore requires splitting App.svelte itself, which
# means extracting the DevTools, Runner, Import, Preferences and OpenAPI-sync
# surfaces into components that can be dynamically imported. That is US-025
# ("Extract inlined modals from App.svelte"), which has not been done.
#
# US-036's own criterion "DevTools, Runner, Import, Preferences, OpenAPI-sync
# and CodeMirror are dynamically imported" has the same dependency: only
# CodeMirror is currently a separate component. The story does not state this
# ordering constraint; it should be run AFTER US-025.
# ---------------------------------------------------------------------------


# ===========================================================================
# Re-measured after US-025 (all 29 dialogs moved onto the shared Modal shell)
# ===========================================================================
#
#   initial chunk  533,898 -> 532,602 bytes   (-1,296)
#
# Essentially unchanged, and that is the expected result rather than a
# disappointment: US-025 replaced each dialog's hand-rolled backdrop/dialog
# WRAPPER with <Modal>, but every dialog's BODY is still inline in App.svelte.
# The wrappers were a few lines each; the bodies are the bytes.
#
# WHAT IT WOULD TAKE to clear the 500,000-byte target, measured rather than
# estimated. The 29 modal bodies total ~57 KB of source in App.svelte:
#
#   3,757  generate-docs          3,649  rename-request      3,613  clone-request
#   3,606  share-collection       3,454  rename-folder       3,350  clone-folder
#   3,337  new-folder             3,336  openapi-spec-diff   3,281  clone-collection
#   2,696  notifications          2,385  openapi-settings    ... 18 more under 2 KB
#
# The remaining gap is 32,602 bytes of MINIFIED output. Moving the bodies into
# lazily-imported components under lib/modals/ would very likely clear it, since
# ~57 KB of source minifies to more than that — but each extraction has to
# thread that dialog's form state and handlers as props, and the large ones
# (the folder/request/collection forms) carry a lot of both. This is a
# component-by-component job, not a scripted one: unlike the wrapper conversion,
# there is no structural pattern a script can derive the props from.
#
# So US-036 remains PARTIAL. The 54% reduction from deferring CodeMirror stands;
# the <500 KB criterion needs the modal bodies extracted, which is the natural
# continuation of US-025 rather than a separate technique.


# ---------------------------------------------------------------------------
# First modal-body extraction, to establish the exchange rate before committing
# to 28 more of them.
#
#   openapi-spec-diff body: 3,336 B of source moved out of App.svelte
#   initial chunk: 532,602 -> 529,719  (-2,883)
#   new deferred chunk: SpecDiffModal 3,865 B
#
# So roughly 0.86 bytes come off the initial chunk per byte of source moved.
#
# PROJECTION for the remaining 28 bodies (~54 KB of source): ~46 KB off the
# initial chunk, landing near 484,000 — under the 500,000 target with ~16 KB of
# headroom. So the approach does close US-036; it is a question of grinding
# through the props, not of finding a different technique.
#
# The dynamic import has to sit INSIDE the {#if} that gates the dialog. Moving
# markup to a new file and importing it statically changes nothing — the
# bundler still puts it in the initial graph. That is the whole trick here and
# it is easy to get wrong in a way that measures as zero.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# ATTEMPTED and REVERTED: automating the remaining 28 body extractions.
#
# The wrapper conversion in US-025 automated cleanly because the structure
# supplied everything a script needed. Body extraction does not, and the reason
# is worth writing down so nobody re-attempts the same shortcut.
#
# The hard part is deriving each dialog's PROPS. A first attempt collected every
# identifier in the block that App.svelte also declares. That over-collects: it
# picked up `source` and `value` for the openapi-settings dialog, which are
# bound LOCALLY inside the block (an {#each} destructuring) and merely happen to
# share a name with an App.svelte local. The generated component then declared
# them as props, and App.svelte tried to forward variables that do not exist in
# its scope — 8 errors, reverted.
#
# Doing it properly means excluding every identifier bound within the block by
# ANY construct: {#each ... as x}, destructured each bindings, {#await ... then
# x}, let: directives, and inline arrow parameters. That is a small scope
# analysis, not a regex, and getting it wrong produces components that typecheck
# in isolation and fail at the call site.
#
# RECOMMENDATION: extract the remaining bodies BY HAND, largest first, one per
# commit with a typecheck between — the same discipline that made the wrapper
# conversion 20/20 clean. SpecDiffModal (committed) is the worked example: 7
# props, ~2,883 bytes off the initial chunk.
# ---------------------------------------------------------------------------

# Hand extraction #2 — generate-docs (the largest body, 3,757 B of source):
#   initial chunk 529,719 -> 527,277  (-2,442); new chunk GenerateDocsModal 3,951 B
#
# Two notes from doing it by hand that the scripted attempt would have got wrong:
#  * bind:this cannot cross a component boundary. The <input> is bound inside
#    the component and surfaced as a bindable prop, with App.svelte keeping its
#    existing reference via bind:selectAllInput — so the indeterminate-state
#    logic there is untouched.
#  * activeCollection had to be typed main.Collection, not a structural
#    {version?: string} & Record<string, unknown>. Collection has no index
#    signature, so the structural type does not accept it.
# Running total: 1,159,933 -> 527,277 (54.5% smaller). 27,277 above target.

# Hand extraction #3 — notifications (2,696 B of source):
#   initial chunk 527,277 -> 525,321  (-1,956); new chunk NotificationsModal 3,468 B
#
# Third case of svelte-check catching something a generated `any` prop would
# have swallowed: setNotificationTab takes App.svelte's local
# NotificationTab = 'all' | 'unread'. Typing the prop as `string` made it accept
# a value the handler cannot take, and the error surfaced at the CALL SITE in
# App.svelte. The union is mirrored in the component instead.
#
# Running total: 1,159,933 -> 525,321 (54.7% smaller). 25,321 above target.
# Three extractions have averaged ~2,400 B off the initial chunk each, so the
# remaining ~26 bodies comfortably cover the gap.

# Hand extraction #4 — share-collection (3,606 B of source):
#   initial chunk 525,321 -> 522,890  (-2,431); new chunk ShareCollectionModal
#
# Second case needing a BINDABLE prop rather than a one-way one: the format
# cards assign to shareCollectionFormat directly, so it must write back to
# App.svelte. Bound with bind:shareCollectionFormat, which leaves App.svelte's
# own reset-to-'zip' logic working unchanged.
#
# Running total: 1,159,933 -> 522,890 (54.9% smaller). 22,890 above target.

# Hand extraction #5 — rename-collection (1,171 B of source):
#   initial chunk 522,890 -> 522,246  (-644)
#
# Note the smaller return: this body is 1,171 B of source against
# share-collection's 3,606 B. The ~2,400 B average from the first four came
# from picking the LARGEST bodies first. Working down the size-ordered list in
# bundle.txt keeps the return per commit as high as it can be; the tail of
# sub-1 KB dialogs will each yield a few hundred bytes.
#
# Running total: 1,159,933 -> 522,246 (55.0% smaller). 22,246 above target.

# Hand extraction #6 — openapi-settings (2,385 B of source):
#   initial chunk 522,246 -> 520,971  (-1,275)
#
# ESLint refused the shortcut, which is worth recording. A helper that generated
# `export let x: any` for every prop tripped @typescript-eslint/no-explicit-any
# (8 errors) and could not be committed. That is the SAME shortcut the abandoned
# scripted extraction depended on — so the repo's own lint config independently
# rules that approach out, not just my judgement about it.
#
# Also: guessing prop names from memory produced two that do not exist
# (openAPISyncSettingsBusy, toggleOpenAPISyncAutoCheck). Read the body and take
# the identifiers from it; do not reconstruct them.
#
# Running total: 1,159,933 -> 520,971 (55.1% smaller). 20,971 above target.

# Hand extraction #7 — clone-collection (3,281 B of source):
#   initial chunk 520,971 -> 519,273  (-1,698)
#
# Method note that saves time on the rest: grepping the body for lines
# containing {identifier}, on:, bind:, disabled= or value= gives the real prop
# list in one pass. Collecting "every identifier the body shares with
# App.svelte's declarations" does NOT — for this dialog it returned 21
# candidates including collection, folder, id, input, label, location, name,
# row and value, all of which are HTML attribute names or locally scoped and
# merely collide with an App.svelte local.
#
# Running total: 1,159,933 -> 519,273 (55.2% smaller). 19,273 above target.

# Hand extraction #8 — new-folder (3,337 B of source):
#   initial chunk 519,273 -> 517,593  (-1,680)
#
# The grep heuristic is a starting point, not an oracle: it missed
# collectionFolderNameIsValid and newFolderDirectoryIsReservedRoot, both used
# inside {#if} conditions rather than on an attribute line. svelte-check named
# both immediately, which is the point — the workflow is "grep to draft the prop
# list, let svelte-check finish it", and the compiler is what makes that safe.
#
# Running total: 1,159,933 -> 517,593 (55.4% smaller). 17,593 above target.

# Hand extraction #9 — rename-folder (3,454 B of source):
#   initial chunk 517,593 -> 516,039  (-1,554)
#
# First one to land with no svelte-check corrections: widening the grep to
# include {#if lines caught renameFolderDirectoryIsReserved and
# collectionFolderNameIsValid up front, which were exactly what the previous
# extraction missed. The heuristic improves as the cases accumulate.
#
# Running total: 1,159,933 -> 516,039 (55.5% smaller). 16,039 above target.
#
# REMAINING BODIES, size-ordered (work top-down):
#   3,649 rename-request    3,613 clone-request    3,350 clone-folder
#   1,587 command-palette   1,472 oauth2-auth      1,427 create-example
#   1,413 global-search     1,349 openapi-spec     1,319 new-request
#   1,255 response-example-code  1,241 item-info    1,112 request-code
#   1,092 delete-request    1,060 delete-folder      942 import-replace
#     912 remove-collection   883 prompt-dialog      640 grpcurl-code
#     521 git-not-found     1,762 unsaved-tabs
# ~29 KB of source against a 16,039-byte gap: comfortable.

# Hand extraction #10 — clone-folder (3,350 B of source):
#   initial chunk 516,039 -> 514,465  (-1,574)
#
# Second clean landing in a row. new-folder / rename-folder / clone-folder are
# near-identical in shape (name draft + filesystem-name toggle + directory
# draft + editing toggle + two validators), so once the first was done the other
# two were transcription. Worth grouping structurally similar dialogs when
# picking the next one.
#
# Running total: 1,159,933 -> 514,465 (55.7% smaller). 14,465 above target.

# Hand extractions #11 and #12 — rename-request + clone-request (7,262 B source):
#   initial chunk 514,465 -> 511,007  (-3,458)
#
# Done as a PAIR in one commit, which is the payoff from grouping by structural
# family. They are the same dialog twice — name draft, filesystem-name toggle,
# filename draft, editing toggle, two validators — differing only in prefix, so
# one generator function produced both components and both call sites, and both
# landed with zero svelte-check corrections.
#
# Ordering note: when extracting two blocks in one pass, do the HIGHER line
# numbers first. Splicing the earlier block first shifts every index after it.
#
# Running total: 1,159,933 -> 511,007 (55.9% smaller). 11,007 above target.

# Hand extractions #13-16 — the four confirmation dialogs
# (delete-request, delete-folder, remove-collection, git-not-found; 3,585 B src):
#   initial chunk 511,007 -> 508,561  (-2,446)
#
# Two corrections svelte-check and eslint made, both the kind that a generated
# component would have carried silently:
#   * deleteFolderTarget.folder needed main.FolderConfig, not a hand-written
#     {name?, path} — the body also reads displayPath.
#   * GitNotFoundModal imports no types at all, so the boilerplate `import type
#     { main }` became an unused-import error.
# Boilerplate generation is fine ONCE the prop list is known; it still has to be
# checked per component.
#
# Running total: 1,159,933 -> 508,561 (56.2% smaller). 8,561 above target.
